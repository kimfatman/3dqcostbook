# T9 · W5/R9 生产验收闭环清单（批次：第二批整改）

> 目标：闭合 2026-08-25 全面审计与 todo.md 中遗留的生产验收项。只做验收与记录，不改生产配置。
> 生产环境：`app.3dq.site`（前端）/ `api.3dq.site`（API）；容器：deploy-app-1（costbook UID 10001）、Caddy、MySQL。
> 执行时机：与 W1–W4 并行；其中 A/B/C 可由脚本化检查完成，D/E 需要用户配合。

## A. 生产登录态 CSV/XLSX 下载验收（遗留 P1）

| 步骤 | 通过条件 | 证据 |
| --- | --- | --- |
| A1 用户登录生产 `app.3dq.site`，进入经营流水页 | 页面正常加载 | 截图 |
| A2 设置任一筛选（如本月支出），点击导出 CSV | 下载成功，文件为 UTF-8 BOM CSV，行数与筛选结果一致 | 文件名 + 行数 + 前两行截图 |
| A3 同筛选导出 Excel | .xlsx 可打开，含冻结表头与金额格式 | 文件名 + 打开截图 |
| A4 空筛选结果导出 | 出现空结果提示或不生成无效文件 | 截图 |

## B. Caddy 安全响应头关键链路复核（遗留 P1）

对以下 URL 逐个记录响应头（HSTS / X-Frame-Options 或 frame-ancestors / X-Content-Type-Options / Referrer-Policy / Permissions-Policy / CSP）与状态码：
1. `https://app.3dq.site/`（应用入口）
2. `https://app.3dq.site/` 任一静态 JS/CSS 资源
3. `https://api.3dq.site/healthz`
4. 登录/注册接口（POST，匿名）
5. 未认证访问私有媒体 URL（应 401/403 而非 200）

通过条件：安全头齐全、匿名媒体被拒、功能正常（登录页可加载、会话访问正常）。可脚本化：`curl -sI` 逐项留档。

## C. 非 root 容器链路验收（遗留 P1）

| 步骤 | 通过条件 |
| --- | --- |
| C1 `docker exec deploy-app-1 id` | UID/GID = 10001（costbook） |
| C2 重启后数据库迁移正常（启动日志无 migration 错误） | 日志证据 |
| C3 登录 → 会话保持（刷新后仍登录） | 截图 |
| C4 上传/预览一张凭证图（COS 私有媒体链路） | 上传成功 + 预览 200 + 未登录访问 401/403 |

## D. 生产登录页无法输入问题排查（用户反馈，未复现）

需要用户提供：受影响设备/浏览器/网络环境，一次录屏或分步截图（聚焦输入框 → 尝试键入）。排查分支：浏览器自动填充冲突 / 焦点被遮罩抢占 / 远程接管输入 / 触控键盘。结论决定是否需要代码修复。

## E. 真机底部导航安全区复核（遗留）

真机浏览器（含底部工具栏与手势条）检查五个一级页：导航不被遮挡、悬浮"记一笔"不挡关键操作、列表末项可触达。需用户手机配合截图。

## 结果归档

全部完成后写入 `docs/batch2-t9-production-acceptance-result-2026-09-XX.md`，并在 todo.md 勾选对应项；B/C 可先行脚本化执行，A/D/E 随用户配合随时补。
