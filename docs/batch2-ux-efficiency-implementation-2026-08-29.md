# 第二批体验与效率整改 · 实施记录

> 实施日期：2026-08-29。流水线：分析 → PRD → 用户确认 → 任务分解 → pi + DeepSeek 逐卡实现 → 独立复验 → 合并 → 本记录。
> 范围确认：用户确认 W1–W5 全做、顺序 W3→W1→W2→W4（W5 并行）。
> 执行引擎：pi coding agent（deepseek-v4-flash，thinking=medium），每卡独立分支 + 主线独立复验。

## 实施总览

| 卡 | 工作流 | 内容 | 提交 | 合并 |
| --- | --- | --- | --- | --- |
| T1 | W3 | 订单/流水/成本卡/智能定价四类详情空态统一「结果—原因—回列表」 | `5fa896f` | `b72edca` |
| T2 | W3 | 成本卡删除降级至更多菜单 + 二次确认（保护逻辑重构为 `cardDeleteBlockReason` 纯函数，行为不变） | `c5d8147` | `9d15e7f` |
| T3 | W1 | 记一笔首屏分层（供应商/备注/凭证折叠）、保存并继续、记住上次支出标签/供应商 | `96d21af` | `6b41722` |
| T4 | W1 | 已传凭证未保存的离开确认（alertdialog，三处导航入口拦截，前端丢弃不调服务端） | `3a2afcb` | `e1c9301` |
| T5 | W2 | 成本结构变化 Top3 + 查看全部、持平冗余词移除、行业参考估算「未入账 · 不计入利润」同行标识、标题周期词去重 | `bbb0b7d` | `25bca40` |
| T6 | W4 | 二级表单统一规格 `secondary-form`/`form-actions`（规格文档 `docs/batch2-t6-form-spec-2026-08-29.md`），应用至供应商/分类/成本卡/BOM/间接成本/预算六表单 | `0026fc7` | `5d62d19` |
| T7 | W4 | 商品排行字段契约回归（销量=数量+单位、利润=￥）、￥/% 单元格式断言、目标完成率 >120% 中性建议 | `13b4fa1` | `51decff` |
| T8 | W4 | 首页近 7 日趋势区分「无交易窗口」（真实空态+补录入口）与「零值日」（真实零渲染）；趋势卡重新接入工作台 | `f873a14` | `07f22d7` |

## 质量门禁

| 检查 | 结果 |
| --- | --- |
| 批次前基线 | 60 文件 / 241 通过 / 1 skip；tsc 0 错误；build 通过 |
| 批次后（main `07f22d7`） | 63 文件 / 274 通过 / 1 skip（+33 项，全部为真实 jsdom DOM 回归）；tsc 0 错误；build 通过 |
| 每卡独立复验 | 每张卡均由主线单独重跑三门禁并逐行核对 diff（含账本库红线抽查：仅 T2 以纯函数重构方式触碰 `cost-book.ts`，行为等价） |
| Playwright e2e | 本机 Windows 因 config.webServer 使用 Unix 环境变量语法无法启动（环境限制，非代码缺陷）；e2e 由 GitHub Actions Linux CI 执行——本批 5 条 main 推送（9805649/25bca40/5d62d19/51decff/07f22d7）CI 全部 success，最终 `07f22d7` CI 绿 |

## 账本红线遵守

金额分存储两位展示、订单成交日/退款日归属、SKU 冻结成本快照、revision 乐观锁、语义色令牌、订单/退款/SKU 正常渲染路径：八卡全部零改动（T2 的 `removeCard` 为行为等价重构，新增 `cardDeleteBlockReason` 仅暴露拦截原因供 UI 展示）。

## 明确不做（本批维持）

批量改价/退款/删除、账本拆表、依赖升级矩阵（19 高危另批）、皮肤收敛、流水长列表虚拟化、虚构经营事件、全局隐式周期联动。

## W5 生产验收状态（T9）

| 项 | 状态 |
| --- | --- |
| B 安全响应头关键链路 | ✅ 2026-08-29 已脚本化复核：app/api/静态资源 HTTP 200，HSTS/CSP/XFO/nosniff/Referrer/Permissions 齐全（证据 `t9b-header-evidence.txt`） |
| A 生产登录态 CSV/XLSX 下载验收 | ⏳ 需用户登录生产操作 |
| C 非 root 容器链路验收 | ⏳ 需服务器访问（本机无 .ssh） |
| D 生产登录页输入问题排查 | ⏳ 需用户受影响环境 |
| E 真机底部导航安全区复核 | ⏳ 需用户真机 |

## 生产发布

本机无服务器访问通道，发布未执行。发布方式沿用既有流程：将 `main@07f22d7` 源码归档解包至 `/opt/cost-book/app`，运行既有发布脚本完成镜像构建、迁移与应用重启，随后验收 healthz / HTTPS 200 / 容器 UID 10001 / 源码标记（含 `form-actions`、`detail-empty-state`、`cardDeleteBlockReason` 等本批标记）。可在用户提供服务器访问方式后由本侧执行，或用户自行运行发布脚本后回填验收结果。

## QA 证据汇总

- 本机全量 vitest：批次前 241 → 批次后 274 通过（每卡合并前独立复跑，9 次全绿）。
- GitHub Actions CI：本批全部 5 条 main 推送 success（含 Linux 侧 e2e）。
- 生产安全头脚本化复核：`docs/batch2-t9b-header-evidence-2026-08-29.md`。

## 后续建议

1. 生产发布后按上文标记做源码级验收并回填本记录。
2. T9 A/C/D/E 四项随用户配合逐项闭环。
3. 暂缓池（依赖升级矩阵、皮肤收敛、长列表虚拟化、账本拆表）建议作为下一批次输入。

