# 移动 App 重构待办

## 图表展示系统升级调研

- [x] 盘点当前首页、成本分析、预算、订单、商品和智能定价的全部图表类型、指标来源、下钻路径与小屏表现，不改动业务口径。
- [x] 调研消费级经营数据产品的趋势、结构、排行、目标进度、风险和对比图表模式，提炼适合移动端成本管理的可用性原则；已补充 Apple、Stripe、Linear、Notion 的官方资料与原则矩阵，见 `docs/dycharts-chart-system-research-2026-08-26.md`。
- [x] 以 Dycharts 的数据映射、图表切换、样式分层、跨设备画幅与交互分享方法为参照，评估现有纯 SVG 图表可安全复用的交互；时间缩放、范围选择、联动十字线、对比、注释与性能能力未获官方公开材料证实，已明确记录为不可作为借鉴依据。
- [x] 形成图表组件体系、交互状态、时间筛选、对比与下钻能力的升级架构，明确现有代码可复用范围与不应改变的数据边界。
- [x] 输出带 P0/P1/P2 优先级的图表系统升级路线、验收指标和预估改动范围；方案见 `docs/dycharts-chart-system-research-2026-08-26.md`，实施前待用户确认。
- [x] 补充至少三个消费级数据产品的官方或一手资料，并将其趋势、结构、排行、目标与风险呈现方式映射到商家成本管理的移动端原则矩阵。
- [x] 对 Dycharts 的时间缩放、范围选择、联动十字线、对比、注释与性能能力逐项核验；官网无法证实时，以“不可验证/不适用”结论入档，不据此设计功能。

## 间接成本与智能定价排版统一

- [x] 逐段审阅用户提供的安卓长截图，定位间接成本分摊与智能测算定价页面在标题、卡片、字段、说明、操作区和底部安全区上的失衡；审阅记录见 `docs/layout-audit-notes-2026-08-25.md`。
- [x] 建立两页与相邻业务页面共用的移动端布局规则：页头、内容最大宽度、卡片内边距、表单行高、双列断点、说明文字和固定操作区。
- [x] 重构间接成本分摊的录入与分摊选择布局，避免字段拥挤、标题与操作并列冲突及选项区视觉跳跃，不改分摊计算与保存逻辑。
- [x] 重构智能测算定价的表单、报价、曲线、模拟和目标档位的垂直节奏，避免大面积深色卡片与密集说明割裂，不改价格/利润计算与滑块逻辑。
- [x] 复核首页、成本分析、间接成本分摊和智能定价在 375px、390px、430px、768px 与 1280px 下的风格、间距和安全区一致性；四页截图结论已记录在 `docs/layout-audit-notes-2026-08-25.md`，并仅为本次实际改动的间接成本/智能定价补充真实点击路径 DOM 回归。

## 全站信息精简与 GitHub 同步

- [x] 盘点一级页、二级页和表单中重复的标题、辅助说明、重复指标及空态提示，保留完成操作与理解口径所需的最小信息；结论见 `docs/information-density-audit-2026-08-25.md`。
- [x] 建立全站信息层级规则：每屏一个主标题、一个主指标/结果、最多一行必要辅助说明，避免导航标题与内容标题重复。
- [x] 在不改变业务逻辑、数据结构、金额口径、计算和可访问名称的前提下，精简重复文案并统一全站页面节奏。
- [x] 对精简后的首页、订单、商品、分析、我的及关键二级页完成页面级回归与多视口复核。
- [x] 核实本地检查点与 GitHub 远程主分支的一致性；已以非强制方式将信息精简检查点 `b1bf4e1132554b45d9c015364f62da66c778db6d` 推送至 GitHub `main`，推送后本地与远程 SHA 一致，记录见 `docs/information-density-audit-2026-08-25.md`。

## 全站消费级图表视觉升级

- [x] 盘点所有运行时图表及其数据来源，建立不改变业务逻辑、数据结构和指标定义的视觉升级边界。
- [x] 建立统一图表设计令牌：品牌主色、成功/风险/预警语义色、金融数字字体、圆角、间距、网格弱化、Tooltip、深色模式与减少动效规则。
- [x] 将所有折线图统一为 3px 平滑曲线、柔和面积渐变和当前/最高/最低点强调；保持真实采样点与现有指标不变。
- [x] 将柱状图、横向排行图和环形图统一为圆角数据载体、简洁图例、核心数值中心化与可读下钻，并去除重复模块标题与无意义装饰。
- [x] 为图表补齐轻量绘制/淡入/数字变化反馈、键盘可达 Tooltip 与深色模式适配，尊重减少动效偏好。覆盖曲线、柱体、卡片、预算环、销售趋势、SKU 排行、成本环心与定价贡献；实际深色截图和回归已归档。
- [x] 新建可复用 ChartTooltip，覆盖折线、柱状/排行与环形图的键盘焦点和触碰式提示，不修改图表数据或下钻逻辑。
- [x] 为图表核心数字补齐可访问的数值变化反馈，并逐一收敛柱状图、排行图、环形图的重复标题和装饰线。定价、预算环、销售趋势、SKU 排行和成本环心已覆盖；短标题和低干扰实线规范已逐图核对。
- [x] 提供用户可操作的图表深色模式切换入口，并复核浅色/深色下的关键图表截图与对比度；切换通过真实 Home DOM 回归，390px 首页、成本分析和智能定价实际深色截图已复核。
- [x] 将数字变化反馈覆盖预算环、SKU 排行与销售趋势汇总，并补采首页、成本分析、智能定价的深色模式效果图与对比度结论。
- [x] 在 375px、390px、430px、平板和桌面复核关键图表页面，输出设计规范、效果图与质量记录；记录见 `docs/chart-visual-upgrade-2026-08-25.md`，含实际深色模式截图级证据。

- [x] 确定四个一级入口：工作台、记账、分析、我的。
- [x] 设计二级页面：行业选择、成本列表、成本详情、记一笔、预算与报表。
- [x] 将桌面侧栏改为移动端底部导航和顶部页面标题栏。
- [x] 为行业模板建立可切换的店铺、分类、成本卡与风险提示状态。
- [x] 完成“工作台 → 记一笔 → 成本列表 → 成本详情”的手机端路径。
- [x] 验证 390px 宽度下的导航、弹层、列表滚动与触控区域。
- [x] 保存重构检查点并更新已发布网站。

## 原型功能差异核查

- [ ] 对照原始记账页，核查收入/支出切换、筛选、搜索、日期、附件、编辑和删除能力。当前功能级证据已汇总于 `docs/prototype-parity-audit-2026-08-21.md`，仍缺旧版逐页对照素材。
- [ ] 对照原始成本卡，核查产品列表、分类、搜索、详情、BOM 配方与成本重算能力。当前功能级证据已汇总于 `docs/prototype-parity-audit-2026-08-21.md`，仍缺旧版逐页对照素材。
- [ ] 对照原始分析与报表，核查月度结构、趋势、排行、报表详情和导出能力。月报详情已支持导出包含经营指标与分类快照的 UTF-8 BOM CSV；当前功能级证据已汇总于 `docs/prototype-parity-audit-2026-08-21.md`，仍缺旧版逐页对照素材与验收结论。
- [x] 月报详情支持导出 UTF-8 BOM CSV，包含经营指标与分类快照；格式转义回归、类型检查、58 项全量测试与生产构建通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，应用容器状态为 `running`。
- [ ] 对照原始供应商与分类管理，核查增删改、分组、搜索与占用校验能力。当前功能级证据已汇总于 `docs/prototype-parity-audit-2026-08-21.md`，仍缺旧版逐页对照素材。
- [x] 输出缺失功能优先级，并在确认后按原有业务流程恢复。已在 `docs/prototype-parity-audit-2026-08-21.md` 输出 P0 旧版逐页素材、P1 逐页结论和 P2 前端包拆分的验收条件；当前未确认核心账本功能缺口，P0/P1 需旧版素材后继续。

## P0：恢复原始账本闭环

- [x] 建立统一业务数据层，恢复原型的记录、产品/服务项目、BOM、分类和供应商对象关系。当前 `BookState`/`useCostBook()` 已按工作区和行业统一承载这些对象，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [x] 恢复记账完整生命周期：收入/支出、日期、商户、附件、流水新增、编辑、删除、搜索和筛选。附件已升级为受保护图片资产并具备成功/失败、移除、保存关联与详情预览的 DOM 回归；生产登录态上传验收继续独立保留。
- [x] 恢复行业成本卡：列表、搜索、分类筛选、详情、成本构成/BOM 增删与成本重算。成本卡、BOM、SKU 同步与订单删除保护已由当前 Store、页面和回归覆盖，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [x] 将概览、预算和分析数据改为从统一业务数据层实时聚合。指标、趋势、预算、分类与报表均由当前行业分录聚合，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。

## P1：恢复经营决策模块

- [ ] 恢复本期/上期成本构成、趋势、TOP5、供应商排行和行业隐性成本分析。供应商排行与当前/上期结构对照已补齐，证据见 `docs/cost-analysis-supplier-structure-2026-08-25.md`；其余历史结构环图交互和完整 Top5 图表组合仍待单独核对。
- [x] 基于当前期间已关联供应商的真实支出分录补充供应商成本排行，并以当前/上期分类结构对照展示增减；没有关联供应商时显示可行动空态，不伪造排行数据。实现、回归和移动端记录见 `docs/cost-analysis-supplier-structure-2026-08-25.md`。
- [x] 恢复月度报表列表、报表详情、成本明细、毛利统计和导出交互。封存报表可在移动端下载 CSV，包含净营收、总成本、毛利、毛利率、经营利润率与分类快照。
- [x] 恢复供应商分组/搜索/CRUD 与分类颜色/占用校验管理。供应商支持搜索、当前行业/共享分组、新增、编辑、删除及历史账本不变性校验；分类支持新增、编辑名称/提示/颜色、占用阻断删除及跨行业占用隔离回归。
- [x] 为供应商管理补齐关键词搜索、编辑、当前行业/共享分组与安全删除提示，并验证历史流水不被改写。供应商可按名称、联系人、分类搜索；支持编辑和跨行业共享；纯函数回归证明编辑/删除仅改变供应商目录，不改写历史流水的 `supplierId` 或封存报表快照。390px 页面复核、类型检查、60 项全量测试与生产构建通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，应用容器状态为 `running`。
- [x] 供应商管理已补齐关键词搜索、编辑、当前行业/共享分组与删除确认提示；390px 页面复核、类型检查、59 项全量测试与生产构建通过。
- [x] 为分类管理补齐编辑名称、提示与颜色的页面路径，明确占用分类不可删除并验证现有流水与报表不被改写。真实 Store 状态转换回归验证编辑仅改变分类目录、保留历史流水与封存报表快照；删除失败时目录、流水和封存报表均保持原引用。390px 页面复核、类型检查、63 项全量测试、生产构建与腾讯云 HTTPS 验收通过，应用容器状态为 `running`。

## 当前恢复迭代

## P0 安全与数据完整性修复

- [x] 审计成本分析页的关键指标、图表、筛选和下钻操作，识别信息层级不清、重复展示和行动入口分散的问题。
- [x] 将成本分析收敛为“成本结论—主要原因—可执行动作”的移动端流程，保留必要的筛选与下钻，不增加冗余说明文字。
- [x] 在智能定价中实现可拖动的长条价格滑块，与保本价、建议价、促销模拟、毛利结果和一键写入同步，并保持两位小数精度。
- [x] 补充成本分析与价格滑块交互回归，完成 390px 复核、生产发布、腾讯云健康验证与 GitHub 同步；最终门禁为 39 个测试文件、133 项测试、TypeScript 检查和生产构建通过，API 健康接口与应用 HTTPS 均返回 200，应用 UID/GID 为 10001。
- [x] 为智能定价价格滑块新增实时预期利润趋势折线图，展示滑块有效价格区间内的单件贡献变化，并突出当前试算价、保本价与建议价。
- [x] 为定价利润趋势图补充价格点采样、两位小数、无有效定价输入与当前价格高亮回归，并在 390px、腾讯云生产和 GitHub 同步后记录验收；最终 TypeScript、40 个测试文件 136 项测试和生产构建通过，API/应用 HTTPS 均返回 200，应用 UID/GID 为 10001。
- [x] 全面审计收入、退款、订单成本、成本卡/BOM、间接成本、预算、毛利率、贡献毛利率、经营利润率与智能定价的核算口径、单位、时间范围和边界条件。
- [x] 审计成本分析首页、结构、趋势、风险复核与下钻入口是否读取同一期间/分类数据，并评估结论、原因与行动文案是否方便商家理解。
- [x] 为确认的核算或分析表达问题实施修复，补充可复算回归、390px 复核、生产验证和 GitHub 同步，并保留带证据的审计记录；全量 TypeScript、40 个测试文件 141 项测试和生产构建通过，生产 API/应用 HTTPS 返回 200，应用 UID/GID 为 10001，审计记录见 `docs/accounting-and-cost-analysis-audit-2026-08-25.md`。
- [x] 在存在成本回冲时，为成本分析首屏补充回冲金额的动态提示；回冲发生时显示“已扣除成本回冲/正向成本”与绿色回冲金额，无回冲时保持“展示项目”紧凑指标。TypeScript、41 个测试文件 143 项测试、生产构建、390px 与腾讯云 API/HTTPS/UID 10001 验收通过，私有 GitHub `main` 同步至 `06fb150c35191697071eedc013de756aa65ac871`。
- [x] 将智能定价的预期利润折线图改为柔和曲线，保持当前试算价、保本价、建议价、零贡献线和精确价格点的可读性与无效输入空态。
- [x] 收紧首页、智能定价、分析与轮播等核心模块在手机、平板和 PC 的宽度、间距、信息密度与断点布局，完成 390px、768px 和 1280px 视觉复核。
- [x] 将产品宣传轮播扩展为五页，制作并接入统一蓝色亚克力质感宣传图，保证文字安全区、轮播指示器、裁切和可访问性在各终端正常显示。
- [x] 为本轮曲线、响应式布局与五页轮播补充回归、生产发布、腾讯云健康验证和 GitHub 同步记录；TypeScript、41 个测试文件 143 项测试、生产构建与腾讯云 API/HTTPS/UID 10001 验收通过，私有 GitHub `main` 与 `37e30473390d153a01a632df54cffe7ac8c62d3a` 一致，记录见 `docs/responsive-pricing-and-promotion-carousel-2026-08-25.md`。
- [x] 盘点并统一优化当前首页销售动能、首页经营趋势与智能定价预期利润等折线类图表，采用柔和曲线、渐变面积、清晰基线与关键点标记，不改变原始数据和计算口径；分析页当前趋势为柱/面积等非折线表达，未误改其图表语义。
- [x] 将首页五页宣传轮播改为无边框、无卡片框的沉浸式承载，保持文字安全区、指示器、五页切换、可访问性和各终端裁切质量。
- [x] 在首页、订单、商品、分析等一级业务页增加安全区避让的悬浮“记一笔”按钮；经营流水二级页复用独立悬浮“记一笔”按钮。390px 已分别复核首页、订单、商品、分析与流水，所有二级/表单页隐藏全局入口，避免与底部导航、固定保存按钮和返回操作冲突。
- [x] 为全站折线图、无界轮播和悬浮记账入口完成 390px、768px、1280px 视觉复核、回归、生产发布和 GitHub 同步；TypeScript、42 个测试文件 145 项测试、生产构建、三档视口与腾讯云 API/HTTPS/UID 10001 验收通过，私有 GitHub `main` 与 `c296d166e45058981670d8e028ebeae74640f09d` 一致。
- [x] 审计成本卡、智能定价、间接成本分摊、订单、退款、预算、报表、账单导出与展示格式中的金额转换、`Math.round`、`ceil`、`floor` 和小数处理。
- [x] 定义统一的金额保留规则：业务金额精确保留两位小数，不采用向上或向下取整；明确边界小数与浮点误差处理方式。
- [x] 修复发现的价格计算与展示偏差，确保成本、售价、毛利、退款、分摊和导出金额遵循同一两位小数口径。
- [x] 补充金额精度回归，完成全量测试、生产发布与腾讯云健康验证；37 个测试文件、128 项测试与生产构建通过，API 健康正常、应用 HTTPS 返回 200、应用 UID/GID 为 10001。
- [x] 将本轮金额精度审计、两位小数定价与展示、分摊余额修复和验证记录同步到私有 GitHub 主分支。
- [x] 在生产智能定价页验证保本价、建议售价和档位价格均保留两位小数，且不再显示价格取整步长；用户已确认通过。
- [x] 审计餐饮、零售、电商、美业服务、小商贩模板切换后首页、订单、商品、成本卡、预算、分析及资料页的项目名称、分类、单位与入口显示是否一致；订单与成本卡路径由五行业生产登录态用户确认，首页/预算/分析/资料由真实 `Home` DOM 回归覆盖，边界与证据见 `docs/template-display-audit-2026-08-25.md`。
- [x] 定位新增成本卡材料行中输入时卡顿/无法连续输入的状态更新、派生计算与重渲染原因，并补充回归用例。
- [x] 修复材料名称、数量、单位与单价的连续输入体验，避免每次按键触发成本卡全页重算或焦点丢失。
- [x] 在 390px 复核模板名称及新增成本卡材料的连续输入，完成 TypeScript、36 个测试文件 124 项测试、生产构建与腾讯云发布；API 健康正常、前端 HTTPS 返回 200、app/Caddy/MySQL 容器运行且应用 UID 为 10001。
- [x] 将本轮模板名称、成本卡图片文案与材料连续输入修复的代码、审计记录同步到私有 GitHub 主分支。
- [x] 在真实模板切换中核对餐饮/美业的订单与成本卡路径，确认实体名称和销售单位显示符合预期；用户已确认通过。
- [x] 在生产已登录环境下分别验证零售、电商、小商贩及餐饮/美业的订单履约单位、订单/退款成本说明与 SKU 技术标识，并记录每个行业的证据；用户已确认五行业订单与成本卡路径均通过，逐行业用户确认表已写入 `docs/template-display-audit-2026-08-25.md`（当前无截图附件）。
- [x] 为行业模板显示补充页面级自动化或快照回归，覆盖底部 Tab、成本卡/SKU 页面、资料页、预算/分析的行业名称、分类及单位来源；映射层回归与真实 `Home` jsdom DOM 回归均已覆盖。生产会话逐页截图仍不是本项的证据范围。
- [x] 为底部 Tab、成本卡页、SKU 页、资料页、预算页和分析页补充真实组件渲染测试，直接断言行业名称、分类文案与单位显示。
- [x] 将页面级模板测试覆盖到五个行业切换场景，并验证预算/分析页实际读取当前模板分类与单位来源；其中小商贩明确验证模板默认单位与既有成本卡单位可不同且均正确显示。
- [x] 使用隔离的 jsdom DOM 测试，沿真实“我的 → 经营行业 → 成本卡/预算/分析”导航路径验证五行业模板显示，避免以纯映射函数替代页面级断言。
- [x] 补充统一账本核心路径的真实 Home DOM 回归，覆盖流水筛选/搜索、记一笔新增/编辑/删除、凭证状态、成本卡搜索空态/恢复及成本结构下钻；审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [x] 将流水“凭证状态”升级为私有图片附件：复用受保护媒体上传、将资产 ID 关联到分录、支持选择/移除/详情预览，并兼容旧分录的布尔凭证标记；迁移、服务端安全、真实组件上传链路与验证记录见 `docs/record-voucher-attachments-2026-08-25.md`。
- [x] 为流水凭证补充真实页面/集成回归：mock `/api/media/upload` 成功与失败，断言文件选择后预览、移除、保存后的 `attachmentAssetId` 关联，以及详情受保护预览入口。
- [x] （P1）已为应用与 API 配置并生产验证 HSTS、CSP、反嵌入、MIME 防嗅探、Referrer 与权限响应头；公开登录/注册页、静态 JS/CSS、健康接口和未认证私有媒体边界复核通过。
- [x] （P1）已将应用镜像改为 `costbook` 专用非 root 用户运行；迁移成功，生产容器配置用户为 `costbook`，进程 UID/GID 为 10001。
- [x] 实施登录、注册与首次初始化的分级限流、失败冷却与通用错误反馈，并覆盖账号/IP 维度的安全回归。
- [x] 将密码策略改为至少 8 位、最长 128 位、拒绝常见弱口令与纯空白密码；同步更新注册/初始化界面提示及自动化测试。
- [x] 修复工作区账本乐观锁保存：仅在条件更新实际影响一行时返回成功，冲突时不写入工作区时间戳或审计事件。
- [x] 将通用 JSON/表单请求体上限收紧至 1MB，保持媒体专用上传路由的 5MB 限制，并覆盖 413 回归。
- [x] 完成 P0 合并发布：安全整合 GitHub 后续功能后，TypeScript、33 个测试文件 119 项测试及生产构建通过；已同步私有仓库并发布腾讯云，API 健康正常、前端 HTTPS 返回 200、app/Caddy/MySQL 容器运行。
- [ ] 在 Caddy 增加最小安全响应头后，复核登录、媒体、静态资源、API 与下载功能仍可用。
- [ ] 以非 root 用户运行生产应用容器后，复核数据库迁移、会话与 COS 媒体链路保持可用。
- [ ] 实现并验证 Caddy HSTS、反嵌入、MIME 嗅探、Referrer、权限与 CSP 响应头后，完成登录、媒体、静态资源、API 与账单下载的生产链路验收。
- [ ] 改造应用 Dockerfile 为构建后以专用非 root 用户运行后，完成静态资源、会话与 COS 媒体链路的生产验证。
- [x] 补充可自动执行的安全头/非 root 回归，完成腾讯云发布、HTTPS 响应头、容器 UID 与健康检查验收。
- [ ] 在生产环境复核 Caddy 安全头上线后的关键链路：登录/注册页加载、已登录会话访问、私有媒体预览、静态 JS/CSS 资源加载、CSV/XLSX 下载均正常，并记录证据后再关闭相关 P1 验收。
- [ ] 为非 root 容器补充链路级验收：验证数据库迁移后应用可正常登录、会话可持续、COS 私有媒体可访问，必要时增加自动化或脚本化检查。

## 全面项目审计

- [x] 建立当前项目版本、测试、构建、生产健康与已知风险的审计基线。
- [x] 审计功能完整性、数据模型与计算口径、关键交互路径、错误恢复及导出流程。
- [x] 审计移动端视觉与可访问性、身份与媒体安全、依赖与部署可靠性。
- [x] 输出带证据的 P0/P1/P2 审计报告与可执行修复清单，并保存可回滚审计版本；详见 `docs/comprehensive-audit-2026-08-25.md`。
- [x] 保存包含 `docs/comprehensive-audit-2026-08-25.md` 与最新 `todo.md` 的可回滚审计检查点后，完成本轮审计归档；检查点为 `000ff79b`。

## 账单数据导出

- [x] 审计经营流水现有月份、类型、渠道、供应商与关键词筛选，以及既有 CSV 导出实现的可复用范围。
- [x] 为当前筛选结果增加 CSV 与 Excel 导出入口，统一导出日期、类型、分类、金额、渠道/供应商、订单与备注等账单字段。
- [ ] 为 CSV 与 Excel 生成器补充筛选、中文文本、金额格式、文件名与空结果回归，并在390px和生产环境验证下载交互；本地390px真实下载已验证为 UTF-8 BOM CSV 和 Microsoft Excel 2007+ 文件；自动化现覆盖筛选、中文/公式文本、正负金额、空结果、安全文件名及 XLSX 工作表冻结/筛选/数值格式，仍需生产会话实测下载。
- [x] 补齐账单导出模型的空结果、安全文件名与真实 XLSX 工作表结构自动化断言；生产登录态下载验收仍按独立待办保留。TypeScript、43 个测试文件 148 项测试与生产构建通过。
- [x] 修复当前账期种子流水在月份序列超过增长系数数组时生成 `NaN` 金额的问题，确保列表与导出文件不包含无效金额；新账期回退最近有效系数，既有演示账本会在初始化时恢复可推导金额。
- [ ] 在已登录的生产环境实际进入经营流水页，分别触发 CSV 与 Excel 导出，并记录下载成功、文件名、文件格式及与当前筛选结果一致的证据后完成生产下载验收。
- [ ] 排查并记录用户反馈的生产登录页无法输入问题：当前自动化浏览器中未复现，输入框未禁用、未只读、未被遮挡，且临时键盘输入/清除可生效；仍需用户受影响环境或移动端触控验证。
- [ ] 在用户实际受影响环境中复现生产登录输入问题，确认是否为浏览器焦点、遮罩、兼容性或远程接管输入问题后，再决定是否需要代码修复。

## 真实手机首页显示问题

- [x] 根据安卓浏览器截图定位并修复核心经营卡中主金额、公式金额、风险说明换行与较上月指标的窄屏显示问题；公式标签提升并解除金额裁切依赖。
- [ ] 复核底部导航在真实浏览器安全区与浏览器工具栏共存时的高度、遮挡和可触达性。本地 375px、390px、430px 已覆盖五个一级页的固定导航、激活态和悬浮入口；未发现直接导航遮挡，记录见 `docs/mobile-bottom-navigation-review-2026-08-25.md`。仍需真实手机浏览器工具栏、手势安全区与滚动末项验证。
- [x] 在 360px、390px 与安卓浏览器等效环境完成首页真实数据状态回归，并发布修复版本；用户已确认真实安卓显示通过。
- [x] 修复首页核心经营卡、公式列、月目标摘要中的数字裁切或视觉不完整问题，确保真实手机宽度下完整读出。
- [x] 重构月销售目标环形图为居中的单一视觉锚点，明确完成率、已完成金额、目标金额与月末预测层级，避免当前环图偏位。
- [x] 替换月目标卡的 CSS 背景圆环为真正居中的 SVG 进度环，消除不同浏览器中圆弧、内层内容与统计列的错位。
- [x] 修复目标保存后的数据链：目标、当月销售额、完成率、预计月末与还需金额必须基于同一真实月份流水聚合，并在保存与流水变化后同步刷新。
- [x] 为目标环新增非零销售额、不同目标值、零目标和超额完成回归，并在360px、390px与真实安卓浏览器验证；计算回归覆盖零销售、40%完成和超额完成，用户已确认实际安卓通过。
- [x] 在真实安卓浏览器登录态归档修复后的首页截图，作为 SVG 月目标环的可审阅验收证据；用户已文字确认显示通过，截图已存入受管静态存储并在视觉记录中引用。
- [x] 清除月目标统计区遗留的“2 列 + 1 行”规则，使目标、预计月末、还需在真实安卓同一行等宽排列，并消除错误的跨行与竖线分隔；用户已在生产安卓浏览器确认通过。
- [x] 审计并恢复用户可见的隐形成本模块：确认数据口径、渲染条件和页面入口，避免近期图表页面分工调整后被误隐藏；已在分析页恢复、通过回归并发布。
- [ ] 在生产登录态验证隐形成本估算列表使用真实当月收入或成本基数生成，并确认“核对流水”进入对应月份。
- [x] 将隐性成本重构为商家可配置的成本池：支持摊位费、房租、水电、人工与自定义间接成本，并与行业基准漏损估算明确区分；方案 A 已实现。
- [x] 为成本池设计简洁的移动端分摊交互，支持按营业额、销量、项目数与自定义比例分摊至用户选定项目；已实现引导式三步配置与项目勾选。
- [x] 为成本池补齐编辑能力与完整回归：支持修改名称、金额、日期、来源、分摊方式、目标项目和手动比例；修改后重算 SKU/定价、不改写历史订单，并覆盖编辑、删除、零基数、比例校验与切换摊销状态。
- [x] 为成本池编辑补充自动化回归，覆盖修改金额、来源、规则、目标项目和手动比例后的 SKU/定价重算。
- [x] 为成本池补充状态回归，覆盖删除设置、暂不摊销到一键摊销、零基数不伪造分配以及历史订单成本快照保持不变。
- [x] 在开发前审计当前隐形成本卡、固定成本记录、SKU/成本卡和定价计算的口径断点，并输出至少两套成本池与项目分摊方案供用户选择；详见 `docs/indirect-cost-allocation-design-options-2026-08-22.md`。
- [x] 按用户确认采用方案 A：每笔间接成本池支持“立即一键摊销”或“暂不摊销”，并允许后续启用或撤销项目分摊而不改写历史订单。
- [ ] 在生产登录态完成成本池冒烟验证：新增实际房租/水电/摊位费，分别验证一键摊销、暂不摊销、恢复摊销、流水入账和新订单成本快照。

## GitHub 仓库导出

- [x] 创建新的私有 GitHub 导出仓库，并配置为当前项目的 `github` 远程地址；因 GitHub 不支持中文仓库名，用户确认使用 `3dqcostbook`。
- [x] 推送当前可复现项目源码、锁定文件、迁移、部署配置、脚本与审计文档；已排除密钥、运行时配置、依赖与构建产物。
- [x] 验证新仓库默认分支、最新提交与远程 URL，并向用户交付仓库链接；远程为 `https://github.com/kimfatman/3dqcostbook.git`，默认分支为 `main`。
- [x] 将导出目标更正为私有仓库 `3dqcostbook`，推送现有项目提交，并删除误建的私有 `-` 占位仓库。

## 首页优化设计方案落地

- [x] 从首页及全局视觉指导中移除网格、虚线账本分隔和“账本蓝图”表达，统一采用用户示例图的干净商业 App 语言；运行时样式复查仅保留连续实线的曲线绘制/进度动画，结论见 `docs/chart-visual-validation-2026-08-25.md`。
- [ ] 将流水凭证图片选择器的虚线边界收敛为细实线焦点态，避免功能控件重新引入虚线装饰语言。
- [ ] 逐项对照用户示例图校准顶部信息区、蓝色经营主卡、目标环、提醒行、营收折线/成本柱状预览、快捷入口、轮播与底部导航的构图与留白。
- [ ] 将全局一级页面卡片改为白底、细边界、轻阴影、16px 圆角与克制蓝色操作色，功能图标统一为克制蓝系。
- [x] 修复旧本地账本状态缺少数组字段时的首页初始化兼容，避免开发预览或历史用户数据出现运行时错误；已补充归一化回归测试。
- [ ] 严格参考用户提供的首页设计方案重构顶部信息区、核心经营结果卡、七日经营趋势与经营目标进度，同时继续使用真实数据口径。
- [ ] 重构经营提醒为异常、风险、机会分级的紧凑列表，并保留可行动下钻入口。
- [ ] 重构经营分析为营收/成本双趋势卡，新增参考方案式快捷入口，并保留现有产品宣传轮播图作为独立模块。
- [ ] 统一首页、订单、商品、分析、我的的背景、卡片、圆角、间距、图标、标题与底部导航规则，确保首页改造不形成孤立视觉。
- [ ] 在 390px 下复核首页模块顺序、数据比例、轮播图、图表、快捷入口与底部导航，并完成全量回归、生产发布和用户验收。
- [ ] 彻底清理 `client/src/index.css` 与 `cashflow-filter.css` 中残留的网格背景、装饰性虚线和不再采用的设计表述，不依赖后置覆盖规则。
- [ ] 在 390px 下完成首页、订单、商品、分析、我的五个一级页面逐页截图验收，并记录与用户示例图的逐模块比对结论。
- [x] 审计五个一级页面的底部导航、标题层级、页面切换、信息优先级和单手操作路径，记录可复现测试证据。
- [x] 审计订单、商品、分析、预算、资料、消息与表单等二级路径的进入、返回、取消、保存、空态、加载态与错误态闭环。
- [x] 输出按 P0/P1/P2 分级的交互与层级结构审计报告，列出影响、复现步骤、证据和建议修复范围，待用户确认后实施。
- [x] 修复历史本地状态中存在成本卡但未生成 SKU 时，订单空态“记录第一笔订单”错误跳转商品页且提示用户新建成本卡的问题；归一化时补齐成本卡关联 SKU，并新增回归测试。
- [x] 修复个人与店铺资料页在查询未完成或失败时永久停留“正在读取”的交互问题，增加错误说明、重试与返回出口；开发预览深链接已复测。
- [x] 将一级 Tab 与关键二级页面的内部 `subPage` 状态同步至 URL/浏览器历史，保证系统返回、刷新和深链接可恢复正确层级；已复测流水、预算、资料、成本卡和订单详情深链接。
- [x] 修复“分析 → 记录成本 → 新增成本”返回时跳过经营流水列表的问题，保留来源页及其筛选上下文；筛选、月份与搜索已序列化到 URL，并完成“流水 → 新增成本 → 返回”复测。
- [x] 补充预算页面的实际交互审计：从首页或“我的”进入，验证返回、编辑、保存、空态/错误态与来源上下文恢复，并将证据补入审计报告。
- [x] 将预算等数值表单的原生英文校验提示替换为中文字段级校验和焦点定位反馈，保持输入错误状态与产品语言一致；0 值提交已在开发预览复测。
- [x] 建立统一的二级页面来源栈，并将五个一级 Tab 与关键二级路径同步至 URL/浏览器历史；根级切换、快捷入口与主要保存收口已接入统一导航语义。
- [x] 修复成本卡编辑、经营流水录入、订单、预算等路径的返回目标，确保回到真实来源页并保留筛选上下文；已复测订单、预算、成本卡、流水和资料错误态的返回出口。
- [x] 为历史成本卡状态补齐可下单 SKU 关联或提供准确的一键修复入口，解除订单空态的错误阻断。
- [x] 为个人与店铺资料查询增加错误态、重试与重新登录指引，避免无限“正在读取”。
- [x] 为成本卡长表单增加取消/返回辅助和未保存修改确认，避免用户在材料编辑中丢失意图；已模拟“继续编辑”选择复测。
- [x] 为页面栈、历史 SKU 恢复、资料加载恢复、长表单取消和中文数值校验新增自动化回归与 390px 移动端验证；89 项回归、构建及开发预览复测通过。
- [x] 为经营流水来源上下文建立可序列化状态，覆盖筛选类型、月份和搜索词，并验证“流水 → 新增成本 → 返回”逐层恢复。
- [x] 将退款、定价写回、资料保存等关键动作纳入浏览器历史同步；根级切换、快捷入口和可精确收口的保存动作已使用页面栈语义。
- [x] 为资料加载恢复、长表单取消保护和中文预算校验分别补充可执行自动化测试，并结合浏览器手工复测验证表现。
- [x] 修复“分析 → 流水 → 新增成本 → 返回”中浏览器历史回退后页面栈被清空、最终错误落到经营首页的问题，并补充逐层回退自动化测试与浏览器复测。
- [x] 定位并修复首页经营数字在 390px 下被截断或显示不完整的问题；金额按万/亿压缩并保留完整金额标签，公式列和指标列均可读。
- [x] 定位并修复首页 Logo/店铺标识区的异常显示；扩大覆盖容器、固定层级，并为私有 Logo 读取失败提供预设图标回退。
- [x] 提供适合移动端经营数据的数字字体选择方案，比较数字宽度、中文协同、金额可读性、授权与性能后由用户确认；用户已选择 A 方案。
- [x] 补齐首页全部产品轮播位，生成并接入带透明亚克力材质、蓝色高光、3D 经营组件元素的宣传图，保证文字安全区与小屏裁切质量。
- [x] 在 390px 下复核数字、Logo、轮播图、指示器和底部导航，完成类型检查、测试、构建与生产发布；腾讯云 API 健康接口返回 `{"status":"ok"}`，前端 HTTPS 返回 200，应用、反向代理与数据库容器均运行。
- [x] 在 390px 下逐一验证三张轮播卡、指示器激活态、左侧文案安全区与右侧亚克力 3D 素材的可见性，并留存检查记录；三张素材均按约 396×86px 全幅渲染，逐一点击后对应指示器获得 `active` 与 `aria-current="true"`。
- [x] 在 390px 下补充首页首屏视觉验收记录，确认 Logo 叠层、经营金额、底部五项导航和通知入口完整可见。
- [x] 清理登录页与已启用业务页面中的网格背景、虚线分隔和虚线装饰条，保留必要的细实线层级。
- [x] 在 390px 下复核登录入口、首页、订单、商品、分析与我的页的实际视觉，归档可审阅结论并重新发布；复核记录见 `docs/mobile-visual-cleanup-2026-08-22.md`。
- [x] 为个人与店铺资料加载错误态补齐与移动端一致的错误卡、重试和返回按钮样式，移除原生黑色控件观感。
- [x] 将登录入口的装饰网格替换为无网格的柔和光晕层，保持商业化蓝色品牌辨识。
- [x] 按用户确认采用 A 方案：将 IBM Plex Mono 固定用于首页金额与图表数字、Noto Sans SC 用于中文正文，并完成小屏金额宽度复核。
- [x] 建立 `index.css`、`cashflow-filter.css` 与 `Home.tsx` 有效类名的对照清单，识别失效、重复和仅靠后置覆盖生效的视觉规则；审计基线与跨文件映射见 `docs/css-layer-audit-2026-08-22.md`。
- [x] 在 CSS 审计文档中补充当前关键类名、定义文件、重复/后置覆盖状态及三类候选规则的代表清单，再完成该项验收。
- [x] 删除或合并已失效的历史 CSS 规则，将当前无网格、细实线、白底承载面等规则回写至其真实样式层；已删除211条未引用规则并归一化62处页面装饰声明。
- [x] 统一首页、订单、商品、分析、我的的背景、卡片、分隔、圆角、标题和金融数字令牌，并在390px下逐页复核。
- [x] 将订单、商品、分析等一级页面残留的 `MiSans`/硬编码 IBM Plex Mono 金融数字声明收敛为 `--financial-numeric-font`，避免与用户确认的 A 方案漂移；共迁移133处声明。
- [x] 清理已失效的背景尺寸、页面装饰声明和淘汰设计措辞，确保剩余图形规则仅服务运行时图表、媒体内容或必要品牌光效；已删除16处无效背景尺寸，根级样式表已无“账本蓝图”表述。
- [x] 形成最终残留图形规则清单，逐条标注图表语义、媒体内容或必要品牌光效用途，并在390px复核后完成验收；详见 `docs/css-layer-audit-2026-08-22.md`。

## 用户身份、店铺资料与视觉资产设计（待确认实施）

- [x] 已确认公开注册并创建个人店铺；首次部署仍由初始化令牌创建管理员，首版不收集联系电话或经营简介。
- [x] 已确认腾讯云 COS 仅用于头像、店铺 Logo 与商品图片，不作为自动备份服务。
- [x] 配置最小权限 COS 子账号、私有 Bucket、地域、访问密钥与应用媒体前缀；仅服务端持有密钥。私有 Bucket 与子账号凭证已通过 HeadBucket 回归验证；媒体键强制使用 `costbook-media/v1/workspaces/{workspaceId}/…` 前缀，上传路由与纯校验器均拒绝缺少工作区或资源主体的请求。
- [x] 为公开注册补充用户、工作区 owner 关系、空账本和会话的一致性事务与安全回归。新增真实注册持久化路径的内存事务回归，验证 user/workspace/owner member/空账本/audit 同时提交，并在写入失败时整体零提交；另验证注册会话主体与新用户一致。
- [x] 为个人头像、店铺 Logo、商品成本图片建立媒体元数据、受保护上传、对象存储引用和替换策略。
- [x] 在“我的”中实现个人与店铺资料编辑，仅包含头像、姓名、店铺名称、行业、联系人和店铺 Logo。
- [x] 在商品成本卡中实现商品图片上传、缩略图和详情主图，确保不改写历史订单成本快照。
- [x] 将首页 Logo 区升级为算得清印章、店铺标识、店铺名、行业账期和经营提醒融合的品牌区，并适配三套皮肤。
- [x] 完成权限、上传、注册、移动端响应式测试、腾讯云生产发布和验收。类型检查、20 个测试文件共 71 项测试、生产构建与 390px 首页复核通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200、应用容器运行且 COS 运行变量已存在。
- [x] 修复生产环境登录后查询 `app_users` 当前用户资料失败的问题，并回归登录、资料与工作区接口。根因是生产迁移账本仅记录初始迁移，缺少 `avatarAssetId`、工作区资料字段和 `media_assets` 表；已补执行既有 Drizzle 迁移，生产表结构与当前查询一致，用户已确认重新登录通过。
- [x] 参照美团、淘宝等移动端商业应用重构自建登录页的视觉层级、品牌承载、输入表单和注册入口。生产端已加载新版入口。
- [x] 修复商品成本图片上传失败，并将成本卡详情上传区压缩为紧凑的图片附加组件。根因是 COS 变量未注入应用容器；已补齐 Compose 映射，用户已确认生产上传、回显成功。
- [x] 在“我的”页补充明确、安全的账户退出入口，并验证会话 cookie 被正确清除。复用服务端 `auth.logout`，二次确认后清理会话；用户已确认生产退出成功。
- [x] 补充注册、权限和上传的可验证验收：覆盖公开注册、登录后资料保存、头像/Logo/商品图片受保护上传成功，以及未登录或缺少工作区/资源主体时被拒绝。自动化回归覆盖注册事务、资料路由及头像/Logo/商品图片 HTTP 成功路径；未登录或缺少工作区/主体时均被拒绝，商品图片上传已由用户在生产实测通过。
- [x] 完成不产生脏数据的生产 smoke test：用户已完成资料保存、图像设置整合与店铺图标优先级验证；生产健康、COS 容器环境、登录、退出及真实商品图片上传均通过。
- [ ] 补充移动端关键路径 390px 可审阅验收记录：逐项记录注册态登录页、`ProfileSettingsPage` 与成本卡图片上传区的实际结论。
- [x] 为 `user_avatar` 和 `workspace_logo` 补充受保护上传成功路径的自动化回归，验证 owner、资源主体、工作区归属与受保护 URL 返回。
- [ ] 补充注册态（非默认登录态）在 390px 下的实际复核证据与结论。
- [x] 补充不产生脏数据的生产 smoke test：用户已在生产环境完成资料页打开、保存原值、头像/店铺标识回填和店铺图标优先级验证。
- [x] 修复生产资料页保存后的报错与显示异常，回归用户资料、店铺资料保存及首页/个人中心回填。首次管理员工作区的缺省联系人已兼容；保存后显式刷新用户与工作区缓存，用户已确认生产资料保存和回填正常。
- [x] 参考用户提供的商家 App 设计稿重构“我的”页：顶部身份卡、店铺/个人资料入口、分组设置、服务入口、个性化与账户区、底部退出操作。生产端已由用户复核通过。
- [x] 为个人与店铺建立可选的品牌化预设头像/店铺图标系统，保留 COS 私有自定义上传并保证主数据回填一致。新增可持久化 `avatarPreset`，私有上传头像优先；用户已确认预设头像选择、保存及“我的”页回填正常。
- [x] 修复腾讯云发布脚本的迁移顺序，确保新生成的 Drizzle 迁移在同一次发布中应用而非延后一版。现先构建应用镜像、后执行迁移、再启动服务；`avatarPreset` 已在同次发布中实际写入生产表结构。
- [x] 为“我的”页补齐并验证参考稿要求的服务入口与个性化分组；390px 截图与生产验收已完成。
- [x] 为店铺侧新增可持久化的品牌化预设图标字段、资料页选择器、回填逻辑与自动化回归；私有 COS Logo 优先级已实现并通过自动化回归，仍需最终生产端优先级验收。
- [x] 修复生产端店铺预设图标选择器和回填未显示的问题。后续按用户要求将入口整合进资料页；最终的可见、可选、保存、回填和优先级验收单列待办。
- [x] 审计并优化个人头像、店铺图标和私有上传图片的分级交互：明确入口、作用对象、展示位置、优先级、替换与回填反馈。
- [x] 将“我的”页改为个人身份与店铺品牌两张独立状态卡，分别显示当前来源、作用范围与进入选择的单一操作。该阶段方案已被用户确认的“单一账户信息卡”方案替代，作为历史方案保留。
- [x] 将用户预设头像和店铺预设图标移至各自独立的选择入口，避免在首页并列呈现导致对象混淆。该阶段方案已被用户确认的资料页分区方案替代，作为历史方案保留。
- [x] 在个人头像、店铺 Logo/预设图标、商品成本图片旁明确资产归属及优先级，并回归“真实上传 > 预设 > 行业默认”的展示规则。
- [x] 将个人头像和店铺品牌的图像设置整合到“个人与店铺资料”页的清晰分区；“我的”页顶部仅保留头像与账户信息卡。用户已确认生产资料页整合通过。
- [x] 补充生产验收：在资料页实际验证店铺预设图标的可见、可选、保存、回填，以及“私有 Logo > 预设图标 > 行业默认”优先级。用户已确认生产端通过。
- [x] 为最终“个人与店铺资料”页分区补充明确的生产验收记录，覆盖个人头像与店铺品牌两类设置的作用范围与保存反馈。用户已确认资料页整合及店铺标识优先级通过。

- [x] 建立移动 App 的统一行业化 Store，并迁入五行业模板、流水、分类、成本卡、供应商和报表种子数据。该项与统一 `BookState`/`useCostBook()` 重复，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [x] 让保存记账真正新增流水、重算月度指标、更新预算和刷新跨页面数据。真实 Home DOM 回归已覆盖新增、编辑与删除，指标聚合由 `ledger-metrics` 回归保护，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [x] 恢复流水筛选、搜索、收入/支出、日期、编辑、删除和凭证状态。真实 Home DOM 回归已覆盖筛选/搜索、成本分录新增、编辑、删除和凭证状态；实际凭证媒体另不在本项范围。
- [x] 恢复行业成本卡列表、搜索、详情、BOM 增删和成本/毛利率重算。该项与已实现的成本卡闭环重复，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。
- [ ] 恢复本期/上期环图、近 6 月趋势、TOP5、供应商排行和隐性成本图表。
- [x] 恢复月报、供应商、分类管理及其 CRUD/校验。封存月报、供应商搜索/CRUD、分类编辑与占用保护均已实现并具备回归，审计证据见 `docs/core-ledger-legacy-todo-audit-2026-08-25.md`。

## 图表模块：移动端保留与重构

- [ ] 恢复成本构成环图，并支持本期/上期切换、分类点击下钻和金额/占比双口径。
- [ ] 恢复近 6 月成本/收入/毛利率趋势图，并支持按月查看数据点和趋势说明。
- [ ] 恢复 TOP5 高成本商品/菜品/服务项目和供应商支出排行图表。
- [x] 恢复五行业的隐性成本可视化及其估算口径说明。五行业模板规则、分析页基数/估算/提示和流水下钻均已实现；真实 DOM 回归覆盖五行业潜在漏损标题。
- [ ] 将图表放入“分析”一级 Tab 的分级页面，不在首页堆叠所有图表。

## 五行业模板规格

- [x] 定义餐饮、零售、电商、美业、小商贩的模板元数据、分类、实体术语和成本口径。
- [x] 定义切换前检查、保留数据、归档数据、重置数据和二次确认规则。
- [x] 定义店铺、流水、成本卡/BOM、供应商、分类、报表和图表的 `industryId` 关联与查询边界。
- [x] 定义模板切换后工作台、记账、分析、成本卡和“我的”页面的刷新策略。

## 功能性与算法科学性审计

- [x] 建立审计口径、风险分级与核心业务不变量清单。
- [x] 审查五行业切换、数据隔离、流水 CRUD、成本卡/BOM、预算、报表、供应商和分类的功能路径与边界条件。
- [x] 复核收入、支出、退款、总成本、预算占用、毛利率、BOM 单位成本、报表快照和隐性成本的计算公式与计量单位。
- [x] 用可复算样本验证跨页面聚合、负向/零值/缺失值、重复操作和行业切换后的结果一致性。
- [x] 输出按严重性排序的审计发现、影响说明、可复核证据与修复建议。

## P0 修复方案细化

- [x] 明确固定基准营收与真实经营收入的切换策略，消除重复确认风险。
- [x] 设计收入、退款、退货回收成本与退款手续费的结构化交易模型。
- [x] 拆分毛利率、贡献毛利率和经营利润率的分类口径、公式与页面指标。
- [x] 用真实期间聚合替换合成趋势/上期系数，并设计月报草稿与封存快照状态。
- [x] 输出 Store、页面、云函数迁移、数据迁移和自动化验证的代码级实施方案。

## P0 修复实施

- [x] 备份现有本地账本状态，并增加可回滚的 schemaVersion 迁移。
- [x] 新增统一交易类型、分类会计角色与分整数金额指标计算模块。
- [x] 将演示基准营收迁移为销售交易，改造收入、退款、手续费和预算写入。
- [x] 以真实期间聚合替换合成趋势与“上月”比例系数，增加月报草稿/封存状态。
- [x] 更新工作台、记账、分析、成本卡和报表的指标名称、口径与空状态。
- [x] 补齐 P0 边界用例、浏览器回归、构建检查并发布新版本。

## 订单、SKU 与退款回收账本

- [x] 定义订单、订单明细、SKU、退款原因、退货回收状态与成本回冲的行业隔离数据模型。
- [x] 将订单销售、订单退款、退款手续费及退货可回收成本转换为关联账本分录。
- [x] 增加订单列表、订单详情、创建订单、SKU 明细和退款处理的移动端页面路径。
- [x] 将 SKU 销量、收入、退款、已售成本、回收成本和毛利接入成本卡、分析与月报。
- [x] 补齐部分退款、多 SKU 订单、不可售退货、可售回收入库和跨行业隔离的自动化回归。售后聚合现按工作区与行业双边界过滤退款；新增多 SKU 部分退款、可售回收/不可售退货、跨行业同 ID 退款隔离回归。类型检查、57 项全量测试与生产构建通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，应用容器状态为 `running`。

## 成本卡闭环与界面统一优化

- [x] 审查成本卡新增、编辑、BOM、SKU 关联与删除保护的缺失路径。
- [x] 实现成本卡新增、编辑、BOM 整体保存、SKU 同步和关联订单保护。
- [x] 统一工作台、列表、详情、表单与空状态的移动端组件规范和视觉层级。
- [x] 完成成本卡 CRUD、SKU 同步、响应式界面、类型检查和发布前回归。

## 成本卡多材料维护修复

- [x] 将成本卡表单的单一基础材料字段改为可动态新增、编辑和删除的材料清单。
- [x] 让成本卡新增和编辑操作一次性保存完整 BOM，并同步重算单位成本、毛利和 SKU。
- [x] 覆盖多材料新增、材料删除、无材料校验和后续订单 SKU 成本同步回归。

## 成本卡智能测算定价

- [x] 明确保本价、目标毛利率、平台费率、履约费用与建议售价的可复算公式。
- [x] 在成本卡详情增加智能定价入口、目标参数输入和多档建议售价。
- [x] 支持一键将建议售价写入成本卡与后续订单 SKU，并保留历史订单快照。
- [x] 覆盖零售价、极端费率、目标毛利率边界、向上取整和售价写入回归。

## 渠道模板、促销模拟与订单预警

- [x] 建立可保存的渠道佣金率、单件履约费和目标贡献毛利率模板。
- [x] 将成本卡智能定价接入渠道模板，并加入竞品价格区间与折扣后利润模拟。
- [x] 在订单创建、订单详情和分析页展示低于保本价或低于目标毛利率的可解释预警。
- [x] 覆盖渠道默认值、模板修改、促销折扣、竞品区间与订单预警的自动化回归。

## 最高标准移动端 UI 质量检查

- [x] 以 390px 与 375px 宽度检查工作台、记账、分析、我的四个一级页面的信息层级、导航稳定性与视觉一致性。
- [x] 检查成本卡、订单、智能定价、预算、报表、供应商和分类管理等二级页面的返回路径、表单可读性、触控尺寸与滚动表现。
- [x] 核对所有金额/百分比等宽数字、蓝图账本识别元素、色彩对比、标题层级、空状态、危险操作与保存反馈。
- [x] 修复高优先级问题，完成移动端截图复核、类型检查、自动化测试、构建检查和发布。

## 专业 UI 设计师全面审视

- [x] 以信息架构、视觉层级、品牌资产、数据可视化、布局节奏和行业化叙事六个维度审视核心页面。
- [x] 以任务完成效率、表单与错误预防、反馈与状态、触控、可访问性及异常状态审视关键操作路径。
- [x] 将问题按体验影响、业务风险与实施成本分级，形成可执行的专业 UI 诊断与设计决策。
- [x] 实施优先级最高的整体化设计优化，并完成小屏复核、质量门禁和发布。

## 行业个性化首页与数据查找体验

- [x] 为餐饮、零售、电商、美业、小商贩定义各自的首页快捷入口、图标、辅助说明与可行动经营洞察。
- [x] 为流水和订单建立按可用自然月筛选、关键词搜索和行业数据边界一致的过滤逻辑。
- [x] 在流水与订单结果中高亮关键词命中片段，并在无结果时给出包含当前月份与关键词的明确反馈。
- [x] 完成五行业、月份切换、搜索高亮、小屏界面、类型检查、自动化测试和构建回归。

## 九宫格页面总览图

- [x] 确定九个覆盖工作台、流水、分析、个人中心及核心业务详情的移动端页面，并定义统一标注格式。
- [x] 截取九张 390px 宽度的页面图，确保均处于可读的代表性状态。
- [x] 拼合品牌化九宫格长图，附页名、简短功能说明、版本与网址标注，并完成可读性校验。

## 基于指导原则的 UI 系统重构

- [x] 将已确认 Logo System Draft 的 Digital Blue #087FF5、Navy #0B1836、圆润线条、店铺轮廓及 + / − / = / 笑脸语义转化为可执行设计规则。
- [x] 重构工作台、分析和一级导航，突出“结果 → 原因 → 判断 → 行动”并清理无意义装饰。
- [x] 重构流水、订单、成本卡和智能定价，建立可解释的算式组件、清晰列表与高效移动端操作。
- [x] 统一间距、圆角、颜色、图标、表单、状态与反馈，完成小屏视觉审阅、类型检查、自动化测试和构建回归。

## 高数据密度移动端体验收束

- [x] 将核心页面视觉比例调整为：数据/图表 60%–70%，操作 15%–20%，解释文字 10%–20%。
- [x] 将工作台与分析页的段落式经营解释压缩为数据标签、单行状态或按需行动入口，并提升趋势与排行的可见面积。
- [x] 收束订单、商品、流水、定价和个人中心的副标题与辅助文本，保留操作必要信息而不形成报告式阅读负担。
- [x] 通过移动端截图、类型检查、自动化测试与生产构建复核高数据密度体验。

## 移动端图表机会调研

- [x] 盘点现有流水、订单、SKU、退款、预算、供应商、成本卡和行业隐性成本数据可支持的图表。
- [x] 调研移动端商家经营图表应回答的核心问题，评估趋势、构成、漏斗、瀑布、热力、排行和预警图的适配性。
- [x] 输出按经营价值、数据完整度、移动端可读性和实现成本排序的图表优先级及页面落位建议。

## 首批经营图表落地

- [x] 基于统一账本口径实现利润瀑布桥、预算燃尽和成本类别环比排行的派生数据与测试。
- [x] 以订单退款真实原因、金额、件数和累计占比实现退款原因帕累托，并在无退款时显示短状态而非伪图表。
- [x] 将图表落位到经营、分析、订单与预算路径，提供预置筛选后的流水或订单下钻。
- [x] 完成 375px/390px 小屏图表可读性、口径边界、类型检查、自动化测试、构建与发布验证。

## 图表去重与页面分工调整

- [x] 明确经营页只展示经营结果与一个趋势入口，分析页只展示利润结果、趋势与成本原因，预算燃尽仅保留在预算页。
- [x] 移除重复横向进度条，改用可区分的结果卡、趋势柱图与成本对比排行，并保留必要下钻。
- [x] 复核经营、分析和预算页面的小屏信息层级、图表任务辨识度、类型检查、自动化测试、构建与发布；腾讯云 API 健康正常、前端 HTTPS 返回 200。
- [x] 审计当前经营、分析、预算三页的图表与状态组件，标记重复表达、唯一决策问题和可复用的下钻入口；详见 `docs/chart-page-role-optimization-2026-08-22.md`。
- [x] 将经营页收敛为经营结果与单一趋势入口，将分析页收敛为利润结果、趋势和成本原因，并将预算燃尽限定在预算页。

## 图表选型再评估

- [x] 为经营结果、预算状态、成本变化、成本构成与退款原因明确各自唯一的决策问题和数据口径。
- [x] 对数字摘要、子弹图、环形图、瀑布图、趋势柱图、排序条形图和帕累托图进行移动端适配比较。
- [x] 输出推荐图形、禁止图形、页面分工与低数据量回退规则，确认后再实施界面调整。

## 最新图表分工 UI 原型

- [x] 固化经营、分析、预算、订单、商品页面各自唯一的数据任务、主图、低数据量回退与下钻入口。
- [x] 按 Digital Blue / Navy 品牌系统重构经营、分析、预算页面的移动端布局，不重复使用同类横条图。
- [x] 重构订单、商品和退款下钻页面，生成完整代表性状态的多页面 UI 原型图。
- [x] 校验图表任务辨识度、信息比例、390px 小屏可读性和原型标注完整性后交付。

## 差异化图表页面真实落地

- [x] 将经营页收束为 KPI 经营结果、单一成本趋势、预算摘要与成本下钻，移除利润桥和预算大图。
- [x] 将分析页重构为有收入时的真实竖向瀑布图、收入/成本趋势与成本环比排行；无收入时显示补录入口。
- [x] 将预算页落地为单环预算进度、月末预测状态和预算编辑，避免与首页和分析页重复。
- [x] 将订单页落实为贡献利润列表和退款状态/帕累托条件展示，将商品页落实为成本卡、单件利润和单位成本微趋势。
- [x] 完成图表下钻、空状态、375px/390px 截图、类型检查、自动化测试、构建与发布验证。

## 五张 UI 原型严格对齐

- [x] 逐项比对经营、分析、预算、订单、商品真实页面与对应原型的页头、标题、区块顺序、图表、列表、状态和主操作位置。
- [x] 严格对齐经营、分析、预算页的视觉尺寸、图表比例、信息密度与色彩层级，同时保留真实数据和无数据回退。
- [x] 严格对齐订单、商品页的筛选、列表行、利润信息、空状态、微趋势和底部导航构图。
- [x] 在 390px 逐页截图与五张原型并排比对，修复差异后通过类型检查、自动化测试、构建和发布验收。

## 经营首页首屏紧凑化

- [x] 移除“本期经营结果”重复口径，将经营利润与金额合并为单一主视觉表达。
- [x] 降低经营利润卡标题、金额、卡片高度和趋势卡标题的视觉尺寸，提升小屏首屏数据密度。
- [x] 在 390px 真机等比视口复核首屏，并通过类型检查、自动化测试、构建和发布验收。

## 首页轻量轮播模块

- [x] 定义仅承载产品宣传与后续广告的首页 Banner，置于经营利润主卡之后、成本趋势之前，不混入经营数据。
- [x] 设计符合 Digital Blue / Navy 系统的宣传 Banner、指示器、手势/按钮操作、自动切换和减少动态偏好回退。
- [x] 在首页顶部 Logo 旁新增长文字加下划线的经营提醒跑马灯，基于真实账本状态生成，点击进入消息中心。
- [x] 新增消息中心二级页面，展示预算、成本、订单与退款等提醒，并支持对应业务页面下钻。
- [x] 在 390px 小屏复核首页层级、Banner 与提醒切换稳定性、触控面积、类型检查、自动化测试、构建和发布。

## 苹果移动端 UI 设计对比调研

- [x] 以 Apple Human Interface Guidelines 与系统应用案例梳理层级、导航、排版、材料、色彩、动效、触控与可访问性原则。
- [x] 审阅算得清经营首页、消息中心及核心列表页，建立与苹果设计体系的逐项对比。
- [x] 区分可直接借鉴的交互原则、应保留的算得清账本蓝图品牌资产，以及不适合移动账本场景的苹果式表达。
- [x] 输出按影响与实现成本排序的优化建议，并确定是否进入下一轮界面改造；详见 `docs/apple-ios-ui-comparison-report-2026-08-21.md`。

## P0 苹果原则适配包技术设计

- [x] 盘点 `Home.tsx` 与 `index.css` 中首页层级、字号、金额、风险状态及可点击控件的现状和改造边界。
- [x] 给出文字/金额/触控令牌、风险语义、减少动态与焦点样式的具体 CSS 和 React 修改建议。
- [x] 绘制首页“行业—成本—预算—风险”经营判断簇的 390px 移动端视觉草图，并定义 Banner、趋势与下钻的相对位置。
- [x] 形成分文件修改清单、伪代码、测试策略、验收指标与回滚边界，待确认后再实际开发；详见 `docs/p0-apple-principles-implementation-spec-2026-08-21.md`、`docs/p0-home-layout-and-interaction-spec-2026-08-21.md` 与 `docs/p0-implementation-rollout-and-acceptance-2026-08-21.md`。

## P0 苹果原则适配包实施

- [x] 新增纯首页经营判断模型与响应式减少动态 Hook，并为风险优先级补充单元测试。
- [x] 将首页经营利润卡改为“行业—成本—预算—唯一优先风险”经营判断簇，保持现有账本数据口径和下钻路径。
- [x] 统一首页与消息中心的关键文字、金融数字、44px 命中区、焦点样式和非颜色风险语义；Banner 降为 96px 次级内容。
- [x] 在 375px、390px、430px 视口及减少动态条件下复核首页和消息中心，并通过类型检查、测试、构建、检查点发布。

## 新 Logo 与 3D 宣传 Banner

- [x] 将用户提供的蓝色“店铺＋算式笑脸”新 Logo 上传为项目静态资产，并替换顶部品牌区、图标与站点图标引用。
- [x] 生成 1 组适配 96px 移动端 Banner 的 3D 核算元素，采用 Digital Blue / Navy、店铺印章、收据、成本卡和算式符号，避免遮挡宣传文案。
- [x] 将 3D 资产分配给三张宣传 Banner，保持 Banner 为次级产品宣传内容，不承载经营数据或抢占首屏核算判断。
- [x] 在 375px、390px、430px 复核 Logo 清晰度、3D 元素裁切、文字对比及质量门禁后发布。

## 全界面层级与任务路径审查

- [x] 盘点经营、订单、商品、分析、我的五个一级入口及全部二级页面的层级、返回路径与关键目的。
- [x] 在 390px 移动端逐页复核首页、订单、商品、分析、我的、消息中心与高频表单/详情，记录实际任务路径。
- [x] 识别重复入口、层级歧义、缺失返回路径、状态不透明、触控与可访问性风险，并按 P0/P1/P2 分级。
- [x] 输出可实施的界面层级优化方案；未获确认前不改变已发布的业务流程；详见 `docs/ui-hierarchy-audit-report-2026-08-21.md`。

## 模块排版布局专项审查

- [x] 建立首页、列表、图表、管理页、详情与表单的排版审查维度：层级、对齐、留白、密度、触控和可读性。
- [x] 在 375px、390px、430px 复核核心模块的列宽、文本截断、操作区、图表占比与底部安全区。
- [x] 识别跨页不一致的卡片节奏、数字列、标题层级、图表留白与表单分组问题，并按 P0/P1/P2 分级。
- [x] 输出模块级布局优化方案；未获确认前不改变现有页面和业务流程；详见 `docs/module-layout-audit-report-2026-08-21.md`。

## 层级与模块布局合并优化实施

- [x] 将订单提醒、Banner 和首页风险的订单下钻统一到一级订单 Tab；为供应商与分类删除补齐一致的确认保护。
- [x] 收口订单空状态、无 SKU 订单表单、分析稀疏图表、成本排行状态列、预算风险色与个人中心待处理区域。
- [x] 调整消息中心条目高度、流水主操作文案、商品趋势/材料表单密度，并统一跨页账本空态、摘要与行组件的布局令牌。
- [x] 在 375px、390px、430px 复核订单、分析、预算、我的、消息、流水与表单；通过类型检查、测试、构建并发布。

## 预算入口与首页露出优化

- [x] 审查首页预算剩余行、经营主卡预算风险、消息中心提醒、个人中心预算设置和预算管理页之间的入口关系。
- [x] 设计首页移除常驻预算卡后的信息策略：首页仅在风险时保留可下钻预算提示，正常状态不额外占用首页空间。
- [x] 在预算管理页展示可直接编辑的月度预算金额输入框、清晰的保存操作与保存后预算/预测的即时刷新反馈。
- [x] 将预算管理收口为“我的”中的主要管理入口，并确保消息中心、经营风险和个人中心均可进入同一预算页面。
- [x] 在 375px、390px、430px 复核首页、消息中心、个人中心与预算页的路径和布局，通过质量门禁后发布。

## 订单页面分组列表样式升级

- [x] 对照用户参考图审查订单行、售后状态、退款列表与筛选空态的行高、图标、数值列、圆角和分隔线。
- [x] 定义订单页统一白底分组列表：左侧蓝色线性图标、中间主副信息、右侧金额/状态与箭头，采用连续细分隔线。
- [x] 将订单成交、售后处理、退款筛选和无结果状态接入同一列表语言，保留利润预警与渠道信息。
- [x] 在 375px、390px、430px 复核筛选、长订单号、金额、状态与底部主操作，完成质量门禁并发布。

## 订单与商品紧凑列表优化

- [x] 审查订单成交行与商品成本卡的字段优先级、当前行高、重复信息和详情下钻边界。
- [x] 定义紧凑行模式：订单保留订单号/客户、成交金额、利润状态与渠道；商品保留名称、单位成本、售价/利润、风险状态与趋势摘要。
- [x] 移除列表内重复操作与高占用趋势图，将完整成本构成、价格模拟、SKU 明细和长期趋势保留在详情页。
- [x] 在 375px、390px、430px 测量多项目可见行数、金额截断、风险识别与 44px 点击区，通过质量门禁后发布。

## 首页经营信息架构头脑风暴

- [x] 盘点现有账本可复算的销量、销售额、利润、成本、退款、库存/SKU 与预算指标，明确哪些可稳定进入首页。
- [x] 按“今天应知道什么、为什么、下一步做什么”排序销量排行、利润排行、成本排行、销售预测与风险提醒。
- [x] 设计首页首屏、首屏以下与分析页的模块边界，避免销售、利润、成本与预测在同一屏重复表达。
- [x] 输出可确认的首页模块组合、线框草图与分阶段改造清单；详见 `docs/home-information-architecture-brainstorm-2026-08-21.md`，待确认后再改动已发布首页。

## 九项经营图表信息架构

- [x] 经营结果保持为纯数字核算结果，不增加图表；核验销售额/订单数双轴趋势、商品销量 Top 5、商品利润 Top 5、成本结构环图和成本趋势堆积面积图的真实数据口径。
- [x] 定义首页首屏、首页下段与分析页的职责：销售动能、目标进度、商品榜单、成本图表、现金流与健康度不重复表达。
- [x] 为月目标完成环、现金流收入支出柱状图和经营健康度雷达图补齐最小可用的目标、现金流分类与评分模型，禁止使用伪造数据。
- [x] 输出每张图的输入字段、计算规则、空状态、下钻路径、线框布局和实施优先级；详见 `docs/nine-chart-home-analysis-spec-2026-08-21.md`，待确认后再进入实际开发。

## 第一批首页经营图表实施

- [x] 新增自然日销售额/订单数、SKU 净销量/真实毛利 Top 5、成本结构的纯聚合函数与单元测试。
- [x] 在首页实现销售额/订单数双轴趋势、商品销量 Top 5、商品利润 Top 5 与成本结构环图，经营结果继续保持数字核算。
- [x] 为每张图接入订单、商品成本卡或流水的真实下钻；订单或 SKU 数据不足时显示可行动空状态，不绘制虚假图形。
- [x] 重排 Banner、成本趋势与第一成本的首页位置，并在 375px、390px、430px 复核图表可读性、金额对齐和触控；通过质量门禁后发布。

## 销售目标与经营趋势图表实施

- [x] 在账本状态中增加按行业保存的月销售目标、迁移与编辑方法；按当前销售额和自然日运行率计算完成率与月末预测。
- [x] 新增月度成本分类堆积序列与现金流流入/流出聚合函数，并为目标、堆积总额和现金流方向添加单元测试。
- [x] 在首页实现销售目标完成环及编辑入口；在分析页实现成本堆积面积图和现金流收入支出柱状图，以及真实数据空状态和下钻。
- [ ] 在 375px、390px、430px 复核图表、目标输入、数字对齐和可行动空状态；本地三档截图已验证首页目标空态、销售目标输入、预算环/预测线与成本分析首屏，记录见 `docs/mobile-sales-target-review-2026-08-25.md`。分析页“趋势与风险复核”展开后的堆积面积图、现金流筛选和下钻仍待可交互会话复核后，再完成类型检查、测试、构建并发布。

## 健康度、目标归档与现金流筛选实施

- [x] 定义并测试经营健康度的公开评分维度、可复算公式、数据完整度阈值与缺口披露模型，不补造缺失数据。
- [x] 在分析页实现健康度雷达图、维度说明和缺失数据行动入口，并接入真实账本下钻。
- [x] 扩展销售目标为按行业、按月归档；增加目标历史、完成率、环比与同月同比查看，缺失可比月份时明确显示原因。
- [x] 为现金流图增加渠道与供应商筛选、对应的可用选项和预置流水下钻；没有可筛维度时不显示伪筛选。
- [x] 在 375px、390px、430px 完成新模块视觉、交互、空状态与数字对齐验收；通过测试、构建并发布。

## 首页显示效果审计

- [x] 采集 375px、390px、430px 首页首屏与全页显示证据，覆盖当前真实账本、目标未设置与订单数据不足状态。
- [x] 审计首页的信息层级、财务数字可读性、图表和空状态、单手操作、底部导航、品牌账本蓝图一致性与响应式边界。
- [x] 输出按 P0/P1/P2 严重度排序的问题清单、截图证据位置、影响说明与可执行修正建议；不在审计阶段直接改版。

## 首页布局显示专项审计

- [x] 按 375px、390px、430px 复核首页模块高度、外边距、首屏折叠、网格和财务列对齐、Banner 安全区与底部导航遮挡。
- [x] 输出独立的布局问题清单，区分结构性问题与数据或文案问题，并给出对应 CSS/组件调整方向。

## 首页经营路径与布局收口

- [x] 核对并修正“总成本 / 已分类成本 / 未分类调整项”的显示关系，避免主结果和成本结构形成无说明的数值差异。
- [x] 合并目标未设与 SKU 订单不足的默认空状态，收束为销售基线缺口并保留唯一优先行动；让 375px 首屏露出成本结构起始信息。
- [x] 统一成本环与分类账本行对齐，增强顶栏风险入口、Banner 控件安全区和内容末尾底部导航安全区。
- [x] 调整 414px 以上宽度的财务列与内部内容密度，维持账本蓝图节奏；完成测试、构建及 375/390/430px 验收并发布。

## 首页模块间距统一

- [x] 定义首页统一尺度：页面横向边距、主/次模块间距、卡片内边距、标题至内容距离、内容至操作区距离及底部安全区。
- [x] 用统一尺度收束主结果、销售基线、成本结构与宣传位的实际布局，保留图表内容需要的最小高度差。
- [x] 在 375px、390px、430px 复核纵向节奏、视觉对齐、首屏折叠与导航安全区；通过测试、构建后发布。

## 有订单首页布局修正

- [x] 以小商贩有订单截图为基准，量化主结果、销售动能图、后续模块和底部导航之间的实际高度、外缘与纵向间距差异。
- [x] 为销售动能图建立独立但受统一节奏约束的标题区、指标区、图表区与日期轴高度，避免占据不成比例的首屏高度。
- [x] 对齐有订单状态下目标、销量排行、利润排行、成本结构与宣传位的模块间距及底部安全区，并在真实小商贩数据中验收后发布。

## 首页紧凑图表与标题统一

- [x] 压缩销量、利润排行单行高度、排名标识和图形条的宽度/厚度，保证多 SKU 时可快速扫描。
- [x] 统一月销售目标完成环与销售动能的模块高度、标题区、数据区和图表区节奏，保留各自必要的数据表达。
- [x] 移除首页模块左上角蓝色短标记，改用稳定的标题基线和账本分隔建立模块识别。
- [x] 在 375px、390px、430px 验收紧凑密度、标题对齐、图表可读性和底部安全区；通过测试、构建并发布。

## 首页轻量账本分隔线

- [x] 定位销量排行、利润排行和成本分类行的黑色直角边框样式来源，确认不影响可点击数据行与进度条。
- [x] 删除两侧和底部硬框线，改用浅灰细横线、首行无分隔和稳定的行内对齐规则。
- [x] 在 375px、390px、430px 验收分隔线层级、图形条、金额列和触控区，通过测试、构建后发布。

## 经营图表动效

- [x] 为首页销售柱线、目标完成环、排行条和成本结构环定义首次呈现与数据更新动效，避免循环或与经营判断无关的装饰动画。
- [x] 为分析页利润瀑布、趋势、堆积面积、现金流柱和健康度雷达增加短时渐进呈现及筛选变化过渡。
- [x] 添加图表卡片按压反馈、`prefers-reduced-motion` 回退和动效时长令牌；在 375px、390px、430px 验收并发布。

## Apple HIG 全面设计审计

- [x] 基于 Apple 官方 HIG 建立清晰度、内容优先、层级、导航、触控、可访问性、动效与状态反馈检查框架。
- [x] 采集经营、订单、商品、分析、我的及关键二级页面在移动端的显示与交互证据。
- [x] 识别与 Apple 设计标准不一致的体验风险，按 P0/P1/P2 排序并给出对应组件、样式或交互修改方向。
- [x] 输出带证据、设计原则映射与可执行修正顺序的 Apple HIG 审计报告；审计阶段不直接改版。

## Apple HIG 首批体验优化

- [x] 将影响经营理解的图例、口径、状态和辅助说明提升至可读字号与对比度；扩大轮播页码、筛选与行内操作的命中区。
- [x] 移除首页宣传轮播暂停组件，保留既有自动轮播和用户主动页码/滑动切换逻辑。
- [x] 将分析页数据不足状态收束为统一状态模板，并将健康度维度公式改为按需展开，减少首屏阅读负担。
- [x] 补齐底部 Tab 安全区与内容底部留白，完成 375/390/430px、减少动态效果、测试、构建与发布验收。

## 模块质感与景深视觉设计

- [x] 审计当前模块背景、阴影、圆角、留白和图表承载面，找出材质单一、前中后景不分和视觉重心平均的问题。
- [x] 提出不少于三套不拘泥于账本元素的质感与景深方向，分别说明光源、材质、层级、色彩、图表承载与动效语言。
- [x] 选定推荐方向，形成首页、分析页、列表、表单和宣传位的模块级空间设计规范；仅在用户确认后实施。

## 可切换视觉皮肤

- [x] 在账本状态中增加视觉皮肤偏好，默认“柔光材质工作台”，并兼容旧本地数据与持久化保存。
- [x] 实现“极光玻璃工作台”“柔光材质工作台”“深海展示舱”三套全局材质、光源、景深和色彩令牌，不改变数据语义。
- [x] 在个人中心提供三套皮肤的可视预览、选择状态与保存反馈，切换后即时应用并保留用户选择。
- [x] 适配首页、分析图表、宣传位、列表、表单和底部导航的皮肤材质；在 375/390/430px 验收可读性、对比度、动效与持久化后发布。

## 计算与输入单元全面审计

- [x] 建立金额、比例、日期、数量、预算、销售目标、成本卡、智能定价、订单、退款和健康度阈值的输入与计算单元清单。
- [x] 审计输入类型、默认值、必填性、最小/最大值、精度、负值、零值、日期边界、单位提示、错误文案和保存反馈。
- [x] 审计预算、销售目标、BOM、保本价、建议售价、订单、退款、成本回收及现金/利润/健康度计算的公式、分单位与跨页面一致性。
- [x] 以真实账本路径验证异常输入、取消保存、重复提交、行业切换和本地持久化；输出 P0/P1/P2 修正清单，不在审计阶段直接修改业务规则；详见 `docs/calculation-input-audit-2026-08-21.md`。

## 输入框占位提示专项核查

- [x] 盘点订单、流水、成本卡、BOM、智能定价、预算、目标、健康度、供应商和分类页面的全部文本与数值输入框。
- [x] 区分有意使用预填默认值、应提供 placeholder、应以标签或单位说明引导的控件，并核对移动端可读性。
- [x] 输出缺失占位提示的修正清单；未经确认不修改既有表单文案或业务逻辑。结论：所有控件均有可见标签或 `aria-label`，但不是所有输入框都有 `placeholder`；日期、数量、预算、退款与模板数值等预填字段无需重复 placeholder，成本卡售价与部分可清空的定价字段可补充示例或单位提示。

## 输入值可删除性专项核查

- [x] 核对所有带 placeholder、`value` 或 `defaultValue` 的输入框，区分自由清空、清空后回退默认值与不可编辑的只读状态。
- [x] 检查数量、费率、材料金额、日期、搜索与目标等受控输入的删除行为及重新输入路径。
- [x] 输出确实无法清空或删除的字段清单与修正建议；未经确认不改动输入状态逻辑。结论：文本、搜索、目标、竞品/折扣与 `defaultValue` 字段可删除；订单数量会在清空时回弹为 1，渠道费率/履约费/目标毛利率会回弹为 0，成本卡材料金额会回写为 0，建议后续允许暂时空值并在提交时校验。

## 数值输入可清空性修复

- [x] 为订单数量、智能定价参数和成本卡材料金额定义可暂时为空的编辑态，并在保存时统一执行数值与边界校验。
- [x] 移除清空后强制回填 1 或 0 的状态更新逻辑，保留已有的默认值、单位提示和提交错误反馈。
- [x] 补充受控数值输入清空/重输的回归测试，完成类型检查、42 项全量测试、构建与移动端表单验证后发布。

## 后端必要性评估

- [x] 盘点当前 localStorage 单设备账本在数据持久化、设备迁移、并发写入、凭证、审计与权限方面的边界。
- [x] 区分原型/单人使用、真实商家长期使用与微信小程序多端协同三种阶段的后端需求。
- [x] 输出推荐的后端能力、数据模型迁移顺序、无需后端的范围和是否立即升级的决策建议；评估阶段不改造现有应用。结论：原型阶段可保持静态，真实商家长期账本和微信多端协同时应优先建设账号、商户隔离、云端账本、服务端校验、附件存储与迁移能力。

## Node.js / Go 后端技术选型与成本评估

- [x] 对比 Node.js 与 Go 在当前 React/TypeScript 账本的领域模型复用、开发效率、服务端校验、并发、微信接入与长期维护方面的适配度。
- [x] 调研适用于低至中等规模商家账本的托管、数据库与文件存储的当前价格和免费额度，并区分开发迁移成本与持续运行成本。
- [x] 输出推荐栈、分阶段建设范围、成本区间与触发 Go 重评估的业务阈值；本轮不直接改造现有应用。结论：首期选 Node.js + TypeScript，Go 保留给以指标证明的结算批处理、外部同步或高并发专项服务；详见 `docs/node-go-backend-evaluation-2026-08-21.md`。

## 自有服务器迁移部署准备

- [x] 确认服务器操作系统、公网网络、SSH 权限、域名/DNS、备案地域与预期部署栈，避免在未知环境中直接执行变更。已确认：腾讯云上海 OpenCloudOS 9、4 核 4 GB、3 Mbps、40 GB 云 SSD、无存量业务；公网 IP `115.159.72.183`；`3dq.site` 已备案并由腾讯云 DNSPod 管理；root 已启用 SSH 密钥登录；接受 Docker 同机部署且无历史账本数据迁移需求。COS 暂不启用，子域采用 `app.3dq.site` 与 `api.3dq.site`。
- [x] 准备最小安全基线、数据库、对象存储、密钥、监控与备份恢复策略，并建立生产/预发布隔离。已完成安全基线、数据库、密钥和容器隔离；用户明确选择暂不配置自动备份或 COS，已知恢复风险已交接。
- [x] 设计现有 localStorage 账本的可确认导入、灰度切换、DNS/HTTPS 发布与回滚流程；本次无历史本地账本需要导入，已完成 DNS/HTTPS 切换并记录容器级回滚边界。

## 腾讯云迁移部署实施

- [x] 确认可用的安全服务器访问路径；不得要求或在对话中接收 root 私钥、密码、数据库密码或 COS 密钥。
- [x] 准备 Node.js 全栈账本、Docker Compose、Caddy、MySQL 与环境变量模板，并在本地/预发布通过验证。
- [x] 经逐项确认后执行服务器用户、补丁、Docker、防火墙、容器与健康检查变更。已停用无业务宝塔组件、启用最小防火墙、创建 `deploy` 账户并部署 MySQL/Node/Caddy；备份自动化作为后续运维增强项保留。
- [x] 经确认后在 DNSPod 添加 `app.3dq.site`、`api.3dq.site` 解析并完成 HTTPS、预发布和正式切换。两个子域 HTTPS 健康接口均返回 200，管理员初始化后登录入口与云端账本快照保存已验证。

## 自建管理员与部署公钥

- [x] 生成仅用于本次服务器部署的 SSH 密钥对；仅交付公钥，私钥仅保留在受控执行环境且不在聊天、仓库或镜像中暴露。
- [x] 提供 root 已有密钥配置下的安全公钥安装命令，并在用户安装后仅进行只读 SSH 环境核验。已确认 OpenCloudOS 9.6、4 核、3.6 GiB 内存、31 GB 可用 SSD、Docker 28/Compose 2.32 可用。
- [x] 将外部服务器部署的认证从托管 OAuth 依赖改为自建管理员账号与受控密码哈希/会话方案，不将初始管理员密码写入代码或环境示例。
- [x] 复核首个管理员已创建后的工作区归属、成员角色、初始化令牌失效与服务端审计事件，不读取任何用户凭据。已确认 1 名管理员、1 个工作区、1 名 owner 及 `workspace.bootstrap`/`book.save` 审计事件；未登录入口已切换为登录页。

## 云端账本首期迁移

- [x] 建立本地管理员、会话、工作区成员、版本化账本快照与审计事件的数据库 Schema，支持首期安全云端持久化和后续规范化拆表。
- [x] 增加自建认证与工作区快照 tRPC 接口，包含首次引导令牌、密码哈希、HttpOnly 会话、工作区授权、乐观版本控制和审计事件。
- [x] 将移动端账本从 localStorage-only 逐步接入受保护的云端读取/保存，并保留网络失败时的本地草稿与清晰状态反馈。
- [x] 生成自托管 Docker Compose、Caddy、MySQL、备份与环境变量模板；不得把任何真实密钥、密码或私钥提交到仓库。

## 服务器核验发现的部署前安全项

- [x] 确认或处置已存在的 Nginx（80/888）、MySQL（公网 3306）与 BT-Panel（公网 8888），避免与成本管家 Caddy/MySQL 冲突及管理面暴露。只读核验确认均由宝塔组件管理：无用户站点根目录、无非系统数据库、无 Docker 工作负载；宝塔服务占约 792 MB 内存及 `/www` 5.2 GB。经确认后已停用且禁用宝塔服务，保留其文件以便回退，端口已释放。
- [x] 在不影响现有访问路径的前提下启用并验证最小防火墙规则：22 受限、80/443 公开，禁止 3000/3306/888/8888 等非业务端口公网访问。已启用 firewalld 并仅开放 SSH、HTTP、HTTPS；当前先保留 22 公网密钥访问，待取得固定来源 IP 后再收紧；独立 SSH 复核成功。
- [x] 在腾讯云安全组入站规则中开放 TCP 80 与 443 至 `0.0.0.0/0`；Caddy 本地监听和 firewalld 已正常，但 Let’s Encrypt HTTP-01 验证仍被云侧网络超时阻断。用户已添加规则，`app.3dq.site` 和 `api.3dq.site` 的 HTTPS 健康接口均返回 200。
- [x] 指导用户从腾讯云实例详情的“安全组”或“网络与安全”入口定位已关联的安全组；控制台界面未显示预期的入站规则入口，需先定位实际入口再添加 Web 端口规则。用户已完成规则添加并通过公网复核。

## 首页模块格式统一

- [x] 定义首页共享模块骨架：页签标题层、等宽数字列、底部操作区与统一圆角/细实线。
- [x] 将主结果、销售基线、成本结构和宣传位对齐到统一的标题、内边距、视觉权重和操作位置；保留主结果与宣传位的必要语义差异。
- [x] 在 375px、390px、430px 验收模块高度、对齐线、CTA 位置、文字截断与底部安全区，通过测试和构建后发布。48 项测试、生产构建与腾讯云 `app/api` HTTPS 健康检查均通过。

## 计算审计 P0 修复（用户确认）

- [x] 修正手工“其他收入”的分录角色，使其不进入毛销售和订单销售趋势，并保持净营收口径正确。
- [x] 在订单退款服务层与页面层限制退款金额不超过订单行成交收入，并校验退款日期不早于订单日期。
- [x] 在订单与退款服务层和页面层强制数量为正整数，拒绝小数、零、负数和非有限值。
- [x] 为收入分类、退款上限/日期和数量边界新增回归测试，通过类型检查、全量测试、生产构建和腾讯云发布验证。新增 `transaction-validation` 测试，现 48 项通过；腾讯云 MySQL 迁移、Node/Caddy 容器与 `app/api` HTTPS 健康接口均通过。

## 计算审计 P1 修复（用户确认）

- [x] 在健康度设置服务层将经营利润率与退款阈值限制为 0–100%，并拒绝非有限值。
- [x] 在渠道费用模板服务层限制佣金率、目标贡献毛利率与履约费的合理范围，拒绝使定价分母失效的组合。
- [x] 统一成本卡与 BOM 材料金额为有限的正金额校验，阻止 0 或异常材料金额进入 SKU 成本。
- [x] 移除固定 `2026-07` 本期和默认日期依赖，采用本地业务日期并在目标、预算、订单、退款和图表中保持一致。
- [x] 补充 P1 边界测试，完成移动端验证、生产构建与腾讯云同步验收。已完成 4 项边界测试、390px 本地业务日期空状态复核、类型检查、52 项全量测试与生产构建；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，MySQL/App/Caddy 容器均正常运行。

## B：订单售后口径收口（用户确认）

- [x] 为订单领域补充退款后净收入、已售/回收成本和经营贡献的可复算聚合口径，并覆盖部分退款、多 SKU、可售回收与不可售退货边界。
- [x] 在订单列表以紧凑且可扫描的方式展示退款后净收入、成本和经营贡献，避免将退款前成交额误作当前经营结果。
- [x] 在订单详情公开订单售后影响拆解：退款、退款手续费、成本回冲、净收入、最终成本与经营贡献，并保留退款状态与下钻入口。
- [x] 为售后口径新增自动化回归，完成移动端验收、类型检查、构建、生产同步与发布记录。已新增 3 项售后回归，类型检查、55 项全量测试、生产构建与 390px 订单空状态复核已通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，应用容器状态为 `running`。

## C：订单、商品与分析页移动端 UI 收口（用户确认）

- [x] 审计订单、商品与分析页的页头、筛选、模块承载面、标题区、数据行、空状态与固定操作区，定义跨页共享问题清单。
- [x] 定义跨页模块规范：模块标题与口径行、连续细实线数据行、等宽数字列、轻量下钻与空状态。
- [x] 以共享规范收口订单、商品与分析页的组件布局、间距、数字对齐与三套皮肤适配，不改变真实数据和现有下钻路径。
- [x] 在 375px、390px、430px 复核三页有/无数据状态，完成类型检查、测试、构建、腾讯云同步和 HTTPS 生产验收。已完成三个视口的订单空状态、商品实数据与分析页面复核；类型检查、55 项测试和生产构建均通过；已同步腾讯云，`https://api.3dq.site/healthz` 返回 `{"status":"ok"}`、`https://app.3dq.site/` 返回 200，应用容器状态为 `running`。
