# 发布前回归记录

## 2026-08-20

开发预览已验证默认电商模板能够显示行业、成本、预算占用和优先风险注记。点击“美业服务”模板后，站点同步切换为美业工作室，月度成本变更为 ¥96,420、预算占用变更为 77.1%、成本分类更新为技师工资/房租工位/耗材产品/获客推广，经营注记与行动入口同步变为工时空置、爽约和耗材管理。

浏览器测试表明行业模板按钮、提示信息以及经营摘要均按当前选择的模板重新渲染。

## 移动 App 重构回归

手机宽度下，首页已经使用四个底部主入口，并将当前行业、电商成本、预算占用、毛利率和优先风险置于首屏经营摘要中。首页不再出现桌面侧栏或整页桌面看板布局。

下一步将继续验证行业模板、记账和成本详情的二级页面路径与返回行为。

行业模板入口现已验证：从工作台进入后，页面显示独立的“切换行业”标题栏和返回按钮，底部四 Tab 被隐藏；五个行业选项显示各自的图标、成本分类预览和确认按钮。该行为符合二级页面栈的预期。

选择并确认“美业服务”模板后，应用返回“我的”一级页面：店铺名称变为美艺工作室，预算变为 ¥125,000，风险提示改为工时空置与爽约，底部四 Tab 重新出现。说明行业状态能跨页面同步应用。

记账底部 Tab 已通过页面事件触发，下一步将确认流水列表、悬浮“记一笔”按钮和记账表单的完整路径。

记账页已验证显示按日期分组的流水和悬浮“记一笔”按钮。点击该按钮后进入独立表单页，顶部出现返回按钮，底部 Tab 隐藏；表单包含金额、当前行业的成本分类和备注字段，符合移动端任务流分级。

在美业服务模板中输入 ¥680 与备注后，保存动作返回记账列表并展示“已保存 ¥680 支出”反馈，验证了移动端记账表单的成功路径。

## 2026-08-20 · 统一数据层恢复

已进入电商模板的新版“记一笔”二级页面。表单现在包含收支类型、金额、日期、8 个电商分类、商户、备注和凭证状态，不再只是单一金额/备注演示输入。

已填写测试广告支出：`¥1,280`、商户“星海广告投放”、备注“7 月直播间素材投放”。下一步将验证保存后真正新增到流水、预算占用和分析派生数据同步刷新。

已选中“广告投放”分类，并确认金额、商户、备注输入均可写入表单。测试过程中的热更新会重置未保存表单，因此后续回归将以保存后的本地持久化结果为准。

受控表单修复后，已从工作台重新进入电商记账页面，准备重复执行“填写 → 切换凭证 → 保存”回归，以确认运行时状态切换不会再丢失表单输入。

测试发现旧渲染方式会在父状态变化时重建内部页面组件，导致受控中文输入仅写入首字符。已改为稳定的内部视图函数调用；类型检查与生产构建均通过，待刷新页面后复测“分类/凭证切换不丢输入”。

刷新开发预览后，应用能重新进入电商记账二级页面，页面栈和字段结构保持正确；下一步将验证稳定渲染下连续填入中文并保存的完整闭环。

稳定渲染修复已通过关键输入回归：完整中文商户“星海广告投放”、金额 `¥1,280` 和备注“7 月直播间素材投放”均可连续写入并保留。下一步测试分类、凭证状态切换和保存回写。

已切换为“广告投放”分类并标记“已附凭证”；金额、商户和备注在两次状态切换后仍完整保留，受控表单回归通过。下一步提交保存并检查流水、预算和分析的同步更新。

测试广告支出已成功保存为当日首条流水，列表展示商户、广告投放分类、备注、凭证状态和 `-¥1,280` 金额，并反馈“已新增 ¥1,280 记录”。进入详情页后可见行业分类、已核算状态、凭证状态以及编辑/删除入口，流水生命周期的保存与详情路径已恢复。

从流水详情返回后，应用正确回到记账一级页面并保持新增记录可见。自动化点击工作台底部 Tab 未触发视图变化，后续将用页面事件和实际触控区域继续复核底部导航的全部入口。

通过页面事件触发工作台导航后，新增 `¥1,280` 广告投放支出已同步反映在工作台：总成本由 `¥138,640` 更新为 `¥139,920`，预算占用由 `92.4%` 更新为 `93.3%`，广告投放分类金额更新为 `¥19,920`。统一数据层的跨页面聚合回归通过。

电商行业成本卡列表已显示商品术语、单位成本和毛利率。进入“轻盈收纳盒”后，详情页展示平台 SKU、售价、成本构成、人工/固定分摊、三项成本构成明细、删除入口、添加成本构成项入口以及近 6 月单位成本趋势，行业成本卡与 BOM 页面路径已恢复。

已从商品成本详情进入独立“添加成本项”页面，并填入“防尘封口贴 / 单件 / 1 件 / ¥1.8”。下一步将提交并核对成本构成、单位成本、毛利率和趋势末点的重算结果。

新增成本项提交后，详情页已增加“防尘封口贴 ¥2”，成本构成由 `30.8` 变为 `32.6`，单位成本由 `38.1 元/件` 变为 `39.9 元/件`，毛利率由 `44%` 变为 `41.3%`。返回成本卡列表后，列表同步显示重算后的成本和毛利率，BOM 计算闭环回归通过。

成本分析页面已展示周期切换、成本与收入的近 6 月趋势、成本构成排序、高成本商品 TOP、供应商支出排行以及电商行业隐性成本拆分。当前电商数据中，隐性成本估算为 `¥15,332`，包括退款退货损失、平台结算漏损、广告无效消耗和库存资金占用；图表模块已恢复。

分析页切换“上月”后，摘要成本更新为 `¥132,224`、收入更新为 `¥202,514`；点击趋势图“成本率”控制后出现“已切换为成本率口径”反馈。周期与图表指标交互回归通过。

从分析页供应商排行的“管理”入口可进入独立供应商管理页，页面展示当前电商行业的四个供应商及其分类、联系人、累计支出和订单数。点击“新增供应商”后进入独立表单，默认分类下拉框仅显示当前行业的八项分类，页面路径和行业隔离口径正确。

新增“智选包装供应”，联系人“王经理”后成功返回供应商列表；新增条目显示为电商行业下的商品采购分类，初始累计支出与订单数均为 `0`，并出现“供应商已新增”反馈，供应商新增闭环回归通过。

尝试删除新增的测试供应商时，浏览器自动化在原生确认框阶段超时并中断会话；重新进入预览后应用首页可正常恢复。该项暂标记为“删除确认待人工/下一轮复测”，不据此判定业务删除功能失败。

工作台“看预算”入口可进入独立预算管理页，正确显示当前电商预算 `¥150,000`、已用 `93.3%`、已用金额与可用余额，并提供预算编辑和 80% 提醒入口。

将电商月度预算调整为 `¥155,000` 并保存后，应用返回工作台并显示“月度预算已更新”；预算占用由 `93.3%` 更新为 `90.3%`，可用余额由 `¥10,080` 更新为 `¥15,080`。预算编辑与跨页聚合回归通过。

底部“我的”Tab 在当前浏览器自动化的普通点击/坐标点击下未发生视图切换，但通过同一页面按钮事件触发后可正常进入“我的”页面，表明应用自身导航逻辑可用，自动化点击命中存在兼容性限制。“我的”页正确汇总当前电商口径：预算 `¥155,000`、成本报表 `3` 期、供应商 `5` 家、分类管理 `8` 类，并提供行业、预算、报表、供应商和分类管理入口。

成本报表列表显示 2026 年 5—7 月的三期电商报表，并注明“模板口径已锁定”。进入 2026 年 7 月报详情后，页内展示成本 `¥138,640`、收入 `¥214,300`、毛利 `¥75,660`、毛利率 `35.3%`，以及八个分类的金额、占比和环比快照；月度报表列表和详情路径回归通过。

在报表详情点击“导出报表”后出现“报表已生成导出任务（演示）”反馈，且返回按钮能正确回到报表列表。当前静态原型保留导出任务入口与反馈，不在浏览器端生成实际下载文件。

从“我的”页面可进入独立分类管理页。电商口径下显示商品采购、平台佣金、履约物流、广告投放、仓储包装、退款售后、人工工资及其他共八项分类，并提供逐项删除入口和“新增分类”入口；分类列表与当前行业模板一致。

回归中发现“新增分类”按钮将子页面状态设置为 `categoryForm`，但页面栈渲染映射遗漏该分支，导致标题显示“新增分类”而内容错误回退到“我的”页。已在 `renderContent` 中补充 `CategoryFormPage` 映射，并验证热更新后表单能正常显示分类名称与颜色字段。填写“内容制作”并保存后，分类列表新增该自定义分类且出现“分类已新增”反馈，分类新增闭环回归通过。

通过确认删除流程移除了无关联的“内容制作”测试分类，分类列表恢复为电商模板的八项基础分类。分类删除的可删除路径回归通过；对已有流水关联的基础分类，数据层仍会返回“无法删除”的保护提示，待后续以支持原生确认框的环境补充界面级复测。

从“我的”页进入“切换行业”后，独立页面列出餐饮、零售、电商、美业服务和小商贩五类模板，并明确提示“历史流水、成本卡和月报保持原行业口径；切换后新记录按目标行业归集”。当前电商模板带有“已选”状态，确认按钮随选择动态显示，行业切换页面与数据隔离说明正确。

选择并确认“美业服务”模板后，应用返回“我的”页并显示“已切换为美业服务模板；历史账本已保留”。店铺切换为美艺工作室，预算为 `¥125,000`，供应商为 `4` 家，分类为 `7` 类，模板切换记录标明最近切换及历史账本已归档保留。行业切换与行业化数据隔离回归通过。

美业服务记账页已显示七条按日期分组的行业流水，包含产品耗材、技师工资、房租水电、营销推广等美业分类。进入“产品耗材结算”详情后，页面展示金额 `-¥18,420`、日期、分类、异常核算状态、凭证状态以及“编辑记录 / 删除记录”入口；已选定该条流水继续完成编辑与删除回归。

点击“编辑记录”后，收支类型、金额 `¥18,420`、日期、产品耗材分类、商户、备注和凭证状态均按原记录回填。将备注更新为“产品 / 耗材（已复核）”并保存后，列表实时显示新备注及“已更新 ¥18,420 记录”反馈，流水编辑闭环回归通过。

为避免删除行业基线流水，已在美业“记一笔”表单中填写一条可清理的测试支出：产品耗材分类、金额 `¥10`、商户“回归测试供应商”、备注“删除功能回归”。下一步将保存该记录并通过删除确认验证列表与成本汇总回滚。

测试流水保存后立即显示在美业流水列表首位，并反馈“已新增 ¥10 记录”。进入详情后确认其金额、分类、备注及未附凭证状态正确；通过删除确认流程移除该条测试流水后，列表恢复为七条基线流水，测试供应商与 `-¥10` 条目不再出现。流水删除闭环回归通过，测试数据已清理。

质量检查已执行：`pnpm run check` 与 `pnpm run build` 均通过。重启开发服务后，当前服务启动正常，类型检查为 0 错误，重启后的日志仅包含启动信息与 Baseline 数据库更新提示；“Missing semicolon”仅存在于重启前的历史日志段，不再是当前源码或生产构建错误。
